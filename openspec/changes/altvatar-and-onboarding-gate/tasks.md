## 1. The table and its migration

- [x] 1.1 Add `profile_avatars` to `db/schema.ts` per design D1: `profile_id` primary key referencing `profiles.id` with `ON DELETE CASCADE`, `style`, `options` jsonb, `art`, `created_at`, `updated_at`. The primary key **is** the one-row-per-profile rule — no separate unique index, and no `ON CONFLICT` target to invent. No column default and no seeding default: the row's absence is what `onboarding-gate` reads.
- [x] 1.2 Add the relation entries beside `profilesRelations` so the table is reachable from the relational query builder, matching how `profile_preferences` is wired.
- [x] 1.3 Generate the migration (`npm run db:generate`) and hand-edit it to repo conventions per `DATABASE.md`: `IF NOT EXISTS` on the `CREATE TABLE`, forward-only, no down-migration, an inline rollback note naming the table drop as the whole rollback. **Additive only** — no backfill, no default row. A backfilled row would silently disable the gate for every existing account, which is the one thing the latch cannot survive.
- [x] 1.4 Confirm the generated migration touches nothing else — no rewrite of `profiles`, `profile_members` or `profile_preferences`, no touch of `users.image` — and that `drizzle/meta/_journal.json` carries only the appended entry.
- [x] 1.5 Apply locally (`npm run db:migrate`), restart the dev server, and confirm the table exists holding zero rows, that every existing profile still renders initials, and that deleting a profile carries its avatar row away by cascade.
- [x] 1.6 Add `profileAvatars` (coarse) and `avatarOfProfile(profileId)` (narrow) to `lib/cacheTags.ts`, following the coarse + narrow contract the file's header comment states. Do not narrow any existing tag — [#309](https://github.com/JoshEddie/CTRLplusList/issues/309) owns that.

## 2. The generation module: vocabulary, styles, resolve, render

> `lib/altvatar/`, split per design D2 so every module stays inside the size bands
> as styles are added and "adding a style" is a new file plus one registry line.

- [x] 2.1 Pin `@dicebear/core@10.7.0` and `@dicebear/styles@10.6.0` exact, and carry no per-style package: v10 publishes all four styles as JSON definitions in one package (design D18). **Re-opened three times:** `adventurer` shipped in the first pass and is dropped, `micah` was added, mapped and dropped in turn (design D15), and the whole set moved off the 9.x per-style packages — none of `@dicebear/avataaars`, `personas`, `toon-head`, `icons` or `shapes` may be left in `package.json` or the lockfile.
- [x] 2.2 Write `lib/altvatar/vocabulary.ts`: the canonical axes and, for each, its canonical values with hand-authored human labels. This is the whitelist — a value not here does not exist as far as the app is concerned. Labels are reviewed text, never derived at runtime: deriving would put `variant07` one missing override away from the screen, which is the failure the vocabulary exists to prevent. **Landed split:** the rows live in `axes.hair.ts`, `axes.face.ts` and `axes.extras.ts` (and `styles/icons.glyphs.ts` for the glyph table) with `vocabulary.ts` assembling them — file size alone, still one canonical style-independent vocabulary (design D2).
- [x] 2.3 Write `lib/altvatar/styles/<style>.ts`, one per style: which canonical axes it has, canonical value → native value for each, and its native defaults for use when a canonical value has no row. Plus the registry the style modules are reached through. (The shipping set changes in section 3; the shape does not.)
- [x] 2.4 Write `lib/altvatar/resolve.ts` — canonical selections + style → the native option object. Pure, no `@dicebear/*` import. Encode that every enum option in the 9.x schemas is an array-typed *pool* (`eyes: ['wink']`, never `eyes: 'wink'`) and that a probability axis is an integer 0–100 (`facialHairProbability: 0` is "no beard").
- [x] 2.5 Implement style resolution in `resolve.ts` per design D3, as the one place it is stated: an axis the style lacks renders no control and keeps its stored value; an axis it has and maps renders it unchanged; an axis it has but **cannot** map keeps its stored value too, drawing the part as absent where the style can express absence and as its own default where it cannot. **Revised:** the first pass overwrote storage with the style's fallback here. That made every style a lossy pass — a style whose hat axis holds one value turned every hat into that hat permanently, because the substitute is what came back. A style change now overwrites nothing; only choosing a value replaces one. An axis holding nothing at all still takes the style's default.
- [x] 2.6 Write `lib/altvatar/render.ts` — native options + style → SVG data URI. The only module importing `@dicebear/*`, loading its style through a dynamic import inside a switch so Next code-splits one chunk per style (design D4). The same module runs on the server for the save path, where the dynamic import is inert. **Two corrections since:** the import is `@dicebear/styles/<style>.json` after the v10 move (17.1/17.2), and the chunk is fetched when `<AltvatarPreview>` mounts rather than when the customizer opens — see 7.8.
- [x] 2.7 Generate with **transparent backgrounds** throughout: `backgroundColor: ['00000000']`, since v10's option schema rejects the `transparent` keyword. The option cannot simply be omitted — left unset, a style paints a background from its own palette. `profiles-surface`'s slot contract paints the accent behind whatever fills the slot, so art carrying its own background would violate it. This is the opposite call from `lib/placeholderArt.ts` deliberately — item art has no owner whose taste can change. Do not copy that module's baked-palette approach, and note that its "must never enter a client bundle" comment is scoped to placeholder art and binds nothing here.
- [x] 2.8 Pass `style: ['default']` for `avataaars`, not `circle`: `circle` draws its own background disc inside the SVG, doubling with the disc the slot already paints.
- [x] 2.9 Confirm `icons` generates glyph-only alpha with the background rect dropped, so CSS masking is viable. The generated SVG carries **two** `fill="#fff"` — the glyph and the `viewboxMask` rect; the mask rect must stay white. Assert the shape the mask path depends on rather than trusting the reconnaissance note. **Superseded by the bump:** that is 9.4.3's markup. v10 stacks two copies of each glyph instead, one at four-tenths opacity, which 17.9's `flattenGlyph` is the answer to — and which is exactly the re-verification the ADR behind 15.10 demands.
- [x] 2.10 Verify the generation module is import-safe from a client component (the customizer's live preview needs it) and that nothing in it reads `db` or a session.

## 3. The contact sheet and the mapping

- [x] 3.1 Add `scripts/altvatar-sheet.ts` (design D10): render every native value of every axis of every style into one static HTML page, each tile captioned with its native key. Dev-only tooling, outside the scoped file-size set.
- [x] 3.2 Seed `avataaars`' vocabulary entries mechanically by de-camelCasing its own already-human names, then review and edit the text that ships. The seeding is an authoring convenience; what lands is reviewed.
- [x] 3.3 Delete `lib/altvatar/styles/adventurer.ts` and every vocabulary row that existed only to serve it: the numbered hair (`long-1`–`long-26`, `short-1`–`short-19`), eyes (`look-1`–`look-26`), eyebrows (`brow-1`–`brow-15`), mouth (`smile-1`–`smile-30`) and glasses (`frame-1`–`frame-5`) values, and the `features` axis outright — no shipping style has it, so it leaves the `EnumAxis` union, `AXIS_ORDER` and `AXIS_TABS` with its rows.
- [x] 3.4 Write `lib/altvatar/styles/personas.ts` and `toon-head.ts`, add each to the registry and to `render.ts`'s dynamic-import switch, and author the canonical values they need that the vocabulary does not already hold. Both name their own parts in words (`bobCut`, `mohawk`, `smirk`, `longBeard`), so this is naming and labelling — not the by-eye matching the dropped style required. `micah.ts` was written alongside them and deleted with the style (design D15).
- [x] 3.5 Add the three axes the new styles introduce — `nose` and `body` (personas) and `rearHair` (toon-head) — each with an `AXIS_ORDER` position beside the part it belongs to, and an `AXIS_TABS` group. `ears` came in with `micah` and left with it.
- [x] 3.6 Remove the `earrings` axis outright. It survived the first pass on `micah`'s two values; with `micah` dropped no shipping style draws earrings, so the axis leaves the `EnumAxis` union, `AXIS_ORDER` and `AXIS_TABS` along with its rows — the same fate as `features`.
- [x] 3.7 Map personas' `cap` and `beanie` as canonical **`hat`** values through the existing `overlay: true` binding rather than as hair. personas files headwear inside its native `hair` axis, which is the shape `avataaars`' `top` already takes — so this needs the mechanism that exists, not a new one. Safe to map only because D3 no longer overwrites: a `hijab` personas cannot draw is left alone rather than resolved onto `cap`.
- [x] 3.8 Ship no colour axis that exactly one style has. `eyesColor` and `eyebrowsColor` were authored for `micah` and left with it; controls are curated rather than exhaustive, and a colour axis one style has is a control that vanishes on every other.
- [x] 3.9 Map `icons`' 146 glyphs to canonical names, or name the subset that ships and let the whitelist exclude the rest. A glyph with no label is not offered.
- [x] 3.10 Confirm the code tolerates an incomplete table end-to-end: a canonical value exists as soon as one style has it, and a style with no row for it simply does not offer it while selected.
- [ ] 3.11 **Owner task**: walk the contact sheet once per new style and confirm each canonical value draws what its label says — `npx tsx scripts/altvatar-sheet.ts && open .altvatar-sheet.html`. Named parts make the table cheap to author and no cheaper to trust; only the owner's eye closes this. **Status:** every mapping was authored against rendered art rather than against the schema, and `micah` was dropped on the strength of what that showed — but `personas` and `toon-head` still want the owner's own pass.

## 4. Deferred handles from acceptance.md

> Each resolves one inline `*TODO:*` marker. A marker with no decision is a deferred
> decision nobody is holding — settle these before the surfaces below are built, so
> the flows can be refined with real handles at the end. Where one handle recurs
> across flows, one decision resolves every instance of it.

- [x] 4.1 Resolve the `*TODO:*` for the gate's **submit control** — its real control text. It sits in `Flow: A new account completes the gate and lands on the page it asked for`, `Flow: Submitting an untouched gate still yields a face and a colour`, `Flow: A blank name is rejected without a write`, `Flow: An existing account is renamed rather than duplicated`, and `Flow: Re-submitting after a partial write succeeds`. One decision, five markers — and it must read sensibly for both populations, since `onboarding-gate` fixes one layout for both.
- [x] 4.2 Resolve the `*TODO:*` for the gate's **cancel control** — its real control text, in `Flow: Cancelling a fresh sign-up raises a confirmation before deleting anything` and `Flow: Cancelling an existing account only signs out`. One control, two behaviours by arm; the spec requires the sign-up arm's wording make unmissable that cancelling abandons the account.
- [x] 4.3 Resolve the `*TODO:*` for the deletion **confirmation's confirm control** in `Flow: Confirming deletes the account and signs out` — the `confirmText` passed to `ConfirmDialog`, which defaults to `Confirm` and should not be inherited by default on a destructive path.
- [x] 4.4 Resolve the `*TODO:*` for the **identity header's avatar edit affordance** in `Flow: The customizer opens on the profile's stored art` and `Flow: The customizer opens on a shuffled face where the profile has none` — the accessible name of the control on `ProfileSpaceIdentity`'s disc that opens the customizer.
- [x] 4.5 Resolve the `*TODO:*` for the **shuffle control** in `Flow: Shuffle re-rolls every curated axis` — its real label.
- [x] 4.6 Resolve the `*TODO:*` for the **style chooser** in `Flow: A style change resolves every axis` and `Flow: A single-choice style is offered as a grid` — how a style is selected and what that control is called.
- [x] 4.7 Resolve the `*TODO:*` for the **customizer's cancel control** in `Flow: Cancelling the customizer keeps the host's values`.
- [x] 4.8 Resolve the `*TODO:*` for the **customizer's confirm control** in `Flow: Confirming the customizer writes nothing until the host submits` and `Flow: The host's submit persists what the customizer returned, and every surface follows`. It returns values to the host and writes nothing, so its wording must not read as saving.

## 5. `FormShell`'s non-dismissible mode

- [x] 5.1 ~~Add the prop to `app/ui/components/FormShell.tsx` suppressing every route out, defaulting to dismissible so no existing consumer changes behaviour.~~ **Superseded by 10.9.** No non-dismissible prop ships. `FormShell` is the shell for a form the viewer may leave; once every exit is removed the two share no behaviour, so the gate renders its own blocking shell instead. The `form-shell-system` delta records the rule.
- [x] 5.2 ~~Leave Escape alone… record the rule at the prop.~~ **Superseded by 10.9.** There is no prop to record it at. The finding still holds — the shell binds no key handler (no `keydown` in `FormShell.tsx` or `use-dismiss.ts`) — and it is the gate's own shell that must hold the property.
- [x] 5.3 ~~Confirm the mode changes nothing else.~~ **Superseded by 10.9.** No mode exists to confirm.
- [x] 5.3b Add `header?: React.ReactNode` to `FormShell` and make `title` optional. A header supplied here replaces the whole `form-shell-hd` title bar and owns its own close affordance — the shell cannot know where in someone else's chrome the control belongs. One node rather than a slot per header part: the alternative is a prop for every element a consumer might move, each with a default the shell keeps rendering. `NewProfileForm` is the first consumer (6.12). Recorded in the `form-shell-system` delta.
- [x] 5.4 Do **not** add a focus trap or `aria-modal`: the gate is not an overlay over content, so there is nothing behind it to trap focus away from. `onboarding-gate`'s "focus moves into the gate" requirement is satisfied by the gate itself, not by the shell. `role="dialog"` **is** carried, on both the gate and the customizer — `HistoryActions`, `StoreFilterPopover` and `PriceFilterPopover` already have it, so this follows the app rather than departing from it, and both the unit suites and the e2e specs locate these surfaces by it.

## 6. The one avatar disc and the read sweep off `users.image`

- [x] 6.1 Add `app/ui/components/ProfileAvatar.tsx` per design D5. **Landed prop shape:** `{ profile, className }` — one `ProfileAvatarView` (`{ name, accent, art, avatarStyle }`) rather than four loose props, so no caller can assemble half a disc, with size carried by the consumer's class rather than an enumerated prop. `facelessView(name)` is exported for a name with no profile behind it. It paints the accent's light stop, then fills: `<img src={art}>` for a figurative style, `mask-image: url(art)` over `background: var(--accent-ink)` for a glyph style, initials otherwise. Masking rather than baking the glyph colour — baking would couple every future accent write to a regeneration.
- [x] 6.2 Carry the native `<img>` lint exemption across but **rewrite its comment**: the reason is now a data URI, not an open-ended third-party origin. Drop `imgFailed`/`onError` — a data URI cannot fail to load — and drop the `FaUser` branch, unreachable once every avatar resolves a profile whose `name` is notNull.
- [x] 6.3 Reuse existing CSS variables for the disc per CLAUDE.md — map to `--accent`-derived tokens and `global.css`'s token set rather than introducing a parallel shorthand system. Style rules live with the disc, not scattered into consumer stylesheets.
- [x] 6.3b Land the unset-accent fallback on a **named preset applied whole** — its two stops, its ink and its shadow — rather than a colour of its own. All five derivations — `accentBackground`, `accentDisc`, `accentDark`, `accentInk` and `accentShadow` — resolve `iris` where no preset is stored or the stored name is one the palette no longer carries, through one `FALLBACK_ACCENT` binding, so the rule has one answer rather than five. An accent-less profile is then drawn by the same code path and held to the same invariants as every stored accent, so no fallback-only colour has to satisfy them separately. In exchange the fallback preset stays selectable and a profile storing `iris` is indistinguishable from one storing nothing — accepted, since the distinction was never visible to a viewer. Recorded as a MODIFIED requirement in the `profiles-surface` delta.
- [x] 6.4 Extend `getMembershipsForUser` in `lib/data/profile.active.ts` with `leftJoin(profile_avatars)`, selecting `style` and `art` alongside the accent it already selects (design D6). Every switcher row and profile card needs the art anyway, so this is not a latch-specific cost. Add `cacheTags.profileAvatars` and `cacheTags.avatarOfProfile(row.id)` to the tags it already carries.
- [x] 6.5 Sweep `lib/data/profile.ts` (4 sites: lines around `getFollowersOfProfile`, `getBlockedByProfile`, `getEligiblePurchasers`, `getProfileForViewer`) off `users.image` and onto `profile_avatars` joined on `profile_id`. All four currently reach the image by joining `profile_members` → `users`; joining the avatar table drops that hop.
- [x] 6.6 Sweep `lib/data/user.ts` (2 sites) the same way.
- [x] 6.7 Repoint `withSelfAvatar` in `lib/data/profile.identity.ts` — the relational-query-builder path — off `user.image`. Its three consumers are `lib/data/item.ts` (×2), `lib/data/list.ts`, and `lib/data/purchase.ts`; `purchase.ts`'s `p.purchaserProfile?.members[0]?.user.image` mapping goes with it. Rename it if `SelfAvatar` no longer describes what it fetches — the art is the profile's, not the account's.
- [x] 6.8 Delete the two now-false "Not cached: reads `users.image` which NextAuth updates out-of-band" comments (`profile.ts`, `user.ts`). **Do not** adopt `'use cache'` on those reads: that is a freshness decision with its own tag audit and design's Non-Goals exclude it. The reads keep their current behaviour.
- [x] 6.9 Repoint every avatar slot at `<ProfileAvatar>`: the nav circle (`app/(auth)/ui/components/UserAvatarPopover.tsx`, both the trigger and the switcher rows), the profile card (`app/(main)/profiles/ui/components/ProfileCard.tsx` — and delete its "until a profile image fills the disc" comment, now false), the identity band (`ProfileSpaceIdentity.tsx`), the list byline (`app/(main)/lists/ui/components/ListDetails.tsx`, 44px), the claims rows (`purchasemodal/ClaimsList.tsx`) and the eligible-purchaser rows (`purchasemodal/ClaimDisclosure.tsx`).
- [x] 6.10 Delete `app/(main)/users/ui/components/Avatar.tsx` and its test once no consumer remains. `initialsOf` in `app/(main)/users/ui/utils.ts` stays — `ProfileAvatar` uses it.
- [x] 6.11 Confirm no application code reads `users.image` anywhere (`grep`). NextAuth keeps writing the column; the column is not dropped — that is a later change with no urgency.
- [x] 6.11b Reshape the two cards the disc changed the anatomy of, and record it here rather than treating it as part of the repoint. `UserCard` gains a `user-card-band` accent-gradient head with the disc straddling its lower edge, its name on `--accent-ink`, and its sub-line always rendered; its `compact` prop is **retired** — `FollowingRail` was its only consumer and now renders the full card, so the two rails no longer draw the same profile two ways. `ListCard` carries `accentVars` on the whole card and gains a byline avatar. Both follow from the disc needing an accent field behind it to sit on; neither is reachable by swapping the avatar source alone.
- [x] 6.12 Leave the layouts and `AccentPicker`'s fieldset form as [#314](https://github.com/JoshEddie/CTRLplusList/issues/314) has them. This change extracts the disc and repoints it. The one exception is the birth form's header: it takes the customizer's own `altvatar-hd` accent band — brand mark plus close, repainting live off the accent the form holds — in place of `FormShell`'s title bar, with an `altvatar-ft` footer of the same family in place of `<FormShellFooter>`, so the band and the footer frame the form as one piece. The mark is the form's own label; a title beside it would name the same thing twice. Recorded in the `profiles-surface` birth-form delta.

## 7. The customizer

- [x] 7.1 Build the customizer as one component with one persistence contract, hosted three ways (gate, birth form, Settings form), opened as a modal *from* its host and returning to it. It renders a live preview wearing the profile's accent, the style chooser from 4.6, the accent picker, and the selected style's curated controls.
- [x] 7.2 Compose controls out of existing primitives per design D9 — nothing new enters the primitive families and no new family spec is owed. **Revised:** the stepper this task originally specified (a `FormField` wrapping previous / next ghost buttons over a label reading the current value) is not what shipped. An enum axis is a grid of tiles, one per canonical value, each drawing the face that choosing it produces; a colour axis is a row of chips over the shared ramp; a glyph style is the same tile grid over glyphs. A stepper makes choosing a linear walk past values the viewer cannot see, which contradicts the premise the customizer rests on — choose by looking — and it is the tile grid that D16 exists to serve. The tiles are plain `<button>`s laid out by the customizer's own CSS, so the "nothing new enters the primitive families" half of this task holds unchanged.
- [x] 7.3 Move `AccentPicker` into the customizer rather than growing a second copy of its markup. Its `TODO(#313)` about hand-copying `FormField`'s label travels with it unresolved — do not fix or duplicate it here.
- [x] 7.4 Open on the profile's stored art where it has any, and on a shuffled result where it has none, so opening a face that already exists never silently replaces it. **Re-opened:** the opening roll now picks the style too (design D14), so the opening roll (`rollAltvatar` in `shuffle.ts`) draws from the figurative pool rather than always opening on `DEFAULT_STYLE` — which returns to being only the fallback for an unrecognised stored id. The roll stays on the surface that renders the host: taken inside a client component it would differ between the server's render and the browser's.
- [x] 7.5 Wire shuffle to re-roll every curated axis **plus the seed** at once. There is no pinned/unpinned state: every axis always holds an explicit value, so nothing has to be unselected and no pool is left wide by design.
- [x] 7.5a Roll the **style** with them, over the figurative styles only — `icons` is excluded, since rolling into a glyph is a category jump rather than a different face (design D14).
- [x] 7.5b Reset the control group on a rolled style, down the same path selecting a style by hand already takes. A group renders only where the selected style has axes in it, so a roll out of a style with clothing into one without would otherwise leave the viewer on a group that no longer exists, looking at an empty panel.
- [x] 7.5c Weight the roll where uniform would misrepresent a value: religious headwear (`hijab`, `turban`) lands at roughly the share of people wearing one rather than at one in eight (design D14). Keep the weighting on the roll alone — every control still offers both alongside every other hat. Verify the resulting distribution rather than assuming it.
- [x] 7.6 Render no control for an axis the selected style lacks, and offer no native value the vocabulary does not name. DiceBear's own names reach neither a control nor storage.
- [x] 7.7 Make the customizer **write nothing itself**: confirming returns `{ style, selections, accent }` to the host and cancelling returns the host's prior values intact. **When** that return is persisted is the host's own decision — a creating host (birth form, gate) commits on its submit, an editing host (the Settings space's identity header) commits on confirm. The customizer is unaware of which it is.
- [x] 7.8 Verified against a clean build **on v10**: each style definition lands in its own async chunk and in no entry chunk — `4714` carries `avataaars` (124 KB raw, 40 KB gzipped), `7568` `icons` (108 KB / 31 KB), `871` `toon-head` (36 KB / 10 KB), `7421` `personas` (32 KB / 7 KB) — and the shared chunks naming a style (`7516` and `8740`, 20 KB each) hold our own canonical→native tables, with zero `@dicebear` references in either. The single-package move costs nothing here: of the sixty definitions `@dicebear/styles` publishes, the fifty-six unused ones appear in no chunk at all, which is what the written-out import paths buy. No chunk under `app/` names a native value from any style, and the dropped `adventurer` and `micah` leave no trace. The task's "only on customizer open" is one step off what ships: the trigger is `<AltvatarPreview>` mounting, which the gate and the Settings form both do before the customizer is opened. What the split is for still holds — one chunk per style, only the selected one, never eagerly — so this is a correction to the wording, not a defect.
- [x] 7.9 Render the Altvatar brand mark in the customizer header with accessible name exactly `Altvatar`, from the raster placeholder (`altvatar-{hor,stacked}-{color,white}.png`). No part of the mark's construction is announced separately. It holds the same box a vector export will, so swapping it is replacing a file (design D13).

- [x] 7.10 Keep the art legible at every skin tone the ramp offers (design D15): a legibility pass in `lib/altvatar/legibility.ts`, on the render path that also derives stored art, correcting by an amount that grows as the skin darkens and reaching nothing before the tones that never needed it. Three rules, one per style — they share the problem and nothing else. Verify against rendered art at every tone, not against the rule.
- [x] 7.11 Lift a covering overlay off the tiles beneath it (design D16). Two canonical axes can share one native part — hair and headwear through avataaars' `top`, eyes and glasses through personas' `eyes` — and the overlay wins, so the grid underneath drew one identical face per value. The face keeps the cover; only the tiles lift it, and the overlay's own grid is untouched.

## 8. The hosts: the birth form, the Settings form, and the accent picker's move

- [x] 8.1 Remove `AccentPicker` from `ProfileFields.tsx` and reach it through the customizer instead. `writeAccent`, `profile_preferences`, `profile.schema.ts` and `useUnsavedChanges` are untouched — only the picker's location changes.
- [x] 8.2 Give `ProfileSpaceIdentity`'s disc the edit affordance from 4.4, rendered for a viewer holding `self` or `owner` and for no one else. A `manager` sees the disc with no affordance, and the Settings form stays fully disabled with no submit control — no disabled submit stands in its place.
- [x] 8.3 Add the Altvatar to `NewProfileForm.tsx` as the third input beside name and tagline, pre-seeded and opening on a randomly-chosen accent preset, so submitting untouched is valid and always produces a profile with a face and a colour. The random choice must not be derived from the creating account, the profile, or any existing value — the form holds none of them, and the profile has no id until creation.
- [x] 8.4 Evaluate whether `AccentPreview.tsx` is subsumed by the customizer's own preview; delete it and its test if so, and say what still consumes it if not.
- [x] 8.5 Write the Altvatar row in `createProfile` (`lib/data/profile.actions.ts`) alongside the accent, in its own statement. Report ignored on purpose, exactly as the accent's is: a profile written without its art renders initials, a state its siblings already define. Fire `cacheTags.avatarOfProfile(id)` with the write.
- [x] 8.6 Split the identity write out of `updateProfileSettings` into `updateProfileIdentity`, which writes the accent and the Altvatar row together — the two commit at a different moment from the fields, so one action committing both was what made every field submit re-write art that had not changed. `updateProfileSettings` keeps the name and tagline columns alone and loses its accent arm with them. Where either half of the identity write fails, report failure **without naming which half**: neither is recoverable by hand and both are fixed by confirming again, so the distinction is one the viewer cannot act on. Fire `cacheTags.avatarOfProfile(profileId)` and `cacheTags.preferencesOfProfile(profileId)` from the writers, plus `cacheTags.profile(profileId)` from the action.
- [x] 8.7 Keep the update action's role gate as the single check for name, tagline, accent and art: a `manager` or non-member is rejected with `Unauthorized` and no profile column, preference row or Altvatar row is written.
- [x] 8.8 Put the avatar read and write in their own pair, `lib/data/profileAvatar.ts` / `lib/data/profileAvatar.write.ts` (design D8) — table-cohesive, and the only placement available since `profile.ts` (413) and `profile.actions.ts` (439) are both at the size ceiling. Do not add endpoints to either. **`.write.ts`, not `.actions.ts`, deliberately:** a `'use server'` module's exports are client-callable endpoints, and `writeAltvatar` writes a profile's identity without checking who is asking — its three callers own that check, so naming it an action would publish it. The accent write has the identical shape and the identical three callers, so it came out alongside as `lib/data/profilePreference.write.ts`. Onboarding takes its own pair over a different subject: `lib/data/onboarding.ts` holds the latch (9.1) and `onboarding.actions.ts`, genuinely `'use server'`, holds the submit and the destructive cancel, which do check.
- [x] 8.9 Derive the rendered art **server-side** from the submitted selections on every save, and never accept a rendering from the client: a client-supplied rendering is arbitrary content displayed to other people. A payload carrying one persists the server's own rendering and drops the submitted one.

## 9. The latch and the layout short-circuit

- [x] 9.1 Add the latch read on top of `getMembershipsForUser`'s result: no `self` row ⇒ arm A (no self-profile); a `self` row with no art ⇒ arm B (backfilled, no art). Pure derivation over rows the read already returns — no extra round-trip, no new query.
- [x] 9.2 Make `app/(main)/layout.tsx` async, resolve the latch, and return the gate **instead of** `<ProfileSwitchProvider><AppFrame>…` for an un-onboarded account (design D7). Not inside the frame: the gate renders on the sign-in gradient with no nav. **Two things this needed that the task did not name:** the segment declares `export const instant = false`, since whether `children` renders at all now depends on a read and the gate's guarantee is exactly a refusal to stream the page optimistically — a Suspense boundary around the frame only hides the same block behind an empty fallback. And `FollowingPage`, which read at its own top level, moved its feed behind a `Suspense` into `FollowingFeed.tsx` so the tab strip still paints immediately; the page itself is synchronous now. No other page under `(main)` is reshaped.
- [x] 9.3 Confirm an unauthenticated request is unaffected — it takes the path it takes today, and the gate does not render.
- [x] 9.4 Confirm the gate is not a route: no URL of its own, no redirect, no `?next=`, no recorded return destination. The requested URL is untouched, so completing the gate reveals the page originally requested.
- [x] 9.5 Cover every route under `(main)` with no allowlist, public content included — a shared list link is gated like every other page. An un-onboarded account can claim nothing, follow nothing and bookmark nothing, so every affordance on such a page is inert regardless.
- [x] 9.6 **Assert that a gated request issues no page-level query.** Because the page element is never included in the layout's output, React never invokes it — but this is a framework property rather than something the code states, and `onboarding-gate`'s "no page work is performed" requirement rests on it. Pin it with a test, per design's Risks.
- [x] 9.7 Verify the layout's read costs no extra round-trip: `authedIdentity` already calls `getMembershipsForUser` in the same request, so `'use cache'` dedupes it. Check this against the source rather than trusting design; a disagreement is a `/run-aground`, not a silent fix.
- [x] 9.8 Add no per-action onboarding guard, and add none later. **Audited rather than assumed** — the guarantee is bounded by the actor-resolution path, and the boundary is stated in the ADR rather than left to be discovered. Every write resolving through `resolveIdentity` (`authedIdentity()` / `authedWriter()`) returns `null` for an account holding no `self` membership, so no profile-scoped write lands. Actions resolving on `authedUserId()` alone are outside it: `createProfile` can mint a profile plus an `'owner'` membership for an un-onboarded account, and the three `visit.actions.ts` writers key rows on the account id. Both are left as they are — the surfaces are unreachable before the gate, the endpoints reachable only by hand, and the worst outcome is an account creating rows only it could have created.

## 10. The onboarding submit and the self-profile mint's move

- [x] 10.1 Move `createSelfProfile` out of `lib/auth.ts`'s NextAuth `createUser` event into `lib/data/profile.self.ts`, imported by the onboarding submit and nothing else. This also gets NextAuth out of the business of knowing the schema. The CTE stays exactly as it is — both rows in one statement is the only atomicity `neon-http` offers, and catching the 23505 **is** the idempotency.
- [x] 10.2 Delete the `?? 'UNTITLED'` fallback from the mint: the submit supplies a name, so the sentinel has no source here. `UNTITLED` survives only as the phase-1 backfill's own sentinel, retired one account at a time by the gate.
- [x] 10.3 Delete the `NamelessAccount_WritesUNTITLEDSentinel` case from `db/__tests__/profiles.test.ts`. **No sweep migration** — no background pass rewrites the name.
- [x] 10.4 Rewrite `createSelfProfile`'s header comment, which today explains why it runs inside the `createUser` event. That reason is gone.
- [x] 10.5 Build the gate's submit action: validate the name against the same bounds a profile name is subject to everywhere else, then mint the profile + `self` membership (CTE), then the Altvatar row, then the accent — in that order, each its own write (design D11). Where a self-profile already exists, update its name rather than creating a second.
- [x] 10.6 Reject a blank or whitespace-only name with a field error on name and **no write at all** — no profile row, no membership row, no Altvatar row.
- [x] 10.7 Leave a partial write recoverable rather than repaired: if the art write fails the account stays un-onboarded, the gate stands, and re-submitting succeeds because the mint swallows its own 23505. Nothing else recovers a partial write and nothing else needs to.
- [x] 10.8 Fire every tag the submit's writes touch — `profilesOfUser(userId)`, `profile(id)`, `avatarOfProfile(id)`, `preferencesOfProfile(id)` — so the gate does not stand after a submission that succeeded. This is the failure the Impact section calls out by name.
- [x] 10.9 Build the gate surface: its own shell on the sign-in gradient, not `FormShell` — a shell with every exit removed shares no behaviour with the shell for a form the viewer may leave, and suppressing them behind a flag leaves that flag with one caller and two meanings. It carries exactly two inputs — the name, pre-filled from whatever name the account already carries, and the Altvatar, pre-seeded and edited through the customizer. No other setting; the copy says so.
- [x] 10.10 Give the gate two copy sets over one layout: the no-self-profile arm reads as finishing sign-up, the existing-account arm does not describe creating an account. Same inputs, same arrangement, for both. Render the brand mark in the headline with accessible name `Altvatar`.
- [x] 10.11 Move focus into the gate when it renders.
- [x] 10.12 Confirm submitting reveals the page originally requested with no manual reload.

## 11. Cancel, by arm

- [x] 11.1 Give the gate one cancel control and no separate sign-out control — at this point the account is still, semantically, in the act of signing in.
- [x] 11.2 Signup arm: raise `ConfirmDialog` naming that the account will be deleted, with the confirm text from 4.3. Nothing is deleted until it is confirmed; declining returns to the gate with the account intact.
- [x] 11.3 On confirmation, delete `users` + `accounts` (cascade) and sign out. The account owns nothing by construction — content hangs off profiles, profiles are reachable only through membership, and guest claims are cookie-scoped with no account linkage at sign-in, so nothing a guest built is destroyed.
- [x] 11.4 Add no ownership check before the destructive path: raised and declined by the owner, because the path runs only where there is no self-profile.
- [x] 11.5 Existing-account arm: sign out only, no confirmation, no deletion — the profile row and every row hanging off it survive.
- [x] 11.6 Put the account-deletion path in `lib/data/user.actions.ts` (97 lines): it is a `users` write, not a profile one.
- [x] 11.7 Introduce no sweep, expiry or background collection for accounts abandoned at the gate. An abandoned account persists un-onboarded indefinitely, meets the gate again on its next request, and can be cancelled away then.

## 12. Seed, local dev and the bypass identities

- [x] 12.1 Give every seeded profile Altvatar art in `scripts/seed-dev-users.ts`, except the two fixtures below. Art is what `onboarding-gate` reads, so a seeded viewer whose self-profile had none would meet the gate on every local page instead of rendering it.
- [x] 12.2 Add two deliberately un-onboarded seeded accounts, one per arm: one holding no membership at all, one holding a self-profile with no Altvatar row. Neither is the primary test viewer. Through the existing `BYPASS_SESSION_USER` seam — no new flag.
- [x] 12.3 Fix `synthesizeSession` in `lib/auth.ts` so a non-default seeded id carries every field actor resolution reads. Today it returns `{ user: { id } }` while `authedUserId` requires `session.user.email`, so every non-default identity resolves to nothing — the `testing-foundation` spec already promises otherwise, and this makes the promise true rather than working around it.
- [x] 12.4 Confirm `db:reset:dev`'s wipe reaches Altvatar rows by cascade, still wipes every profile a seeded user holds any membership on, and still precedes the seeded-user delete.
- [ ] 12.5 Run `npm run db:reset:dev`, restart the dev server, and confirm both un-onboarded fixtures come back un-onboarded, the primary test viewer renders `/lists` rather than gating, and neither fixture is the viewer. **Left to the owner:** the reset cascade-wipes the local dev database including UI-created rows, and the dev server it needs restarted is theirs. The same seed is proven against the Docker e2e database by the run behind 16.5.
- [x] 12.6 Update `LOCALDEV.md`: extend `### Profile coverage` with the Altvatar rows and the two un-onboarded fixtures, and name the two identity-selector values in `## Session identity (BYPASS_SESSION_USER)` so both arms are previewable locally.
- [x] 12.7 Remove the seed's `avatar()` data-URL helper and the `image` field on `seedUsers` if nothing reads `users.image` any more; keep them and say what still reads them if something does. Its comment already names `UserImage`, which is gone.
- [x] 12.8 Repoint the seed's three `adventurer` fixtures (`alice`, `eve`, `hank` in `scripts/seed-dev-users.ts`) onto the new styles, so every shipping style has a seeded example and every fill of the avatar disc stays on screen at once. Update `LOCALDEV.md`'s `### Profile coverage`, which names the styles by hand.

## 13. Unit tests

- [x] 13.1 Cover style resolution directly (`resolve.ts`): a shared canonical value survives a style change; a value the target style **cannot** map is kept rather than overwritten, and comes back when a style that draws it is selected again; an axis the target style lacks renders no control and keeps its value; an axis holding nothing takes the style's default; a colour axis carries verbatim. Cover what reaches the library too: an unmappable value on an axis that can be absent writes probability 0, on an overlay leaves the axis beneath intact, and on an axis that cannot be absent writes the style's own default.
- [x] 13.2 Cover the whitelist: a native value the vocabulary does not name is not offered by any control and cannot be produced by any selection, and no library-native name reaches a label or storage.
- [x] 13.3 Cover the save path's server-side derivation: a payload carrying a rendering alongside its selections persists the server's rendering and not the submitted one. This is the security-relevant leg — it must fail if the guard is removed.
- [x] 13.4 Cover the latch in both arms and both negatives: no `self` row ⇒ un-onboarded; `self` row with no art ⇒ un-onboarded; `self` row with art ⇒ onboarded; a managed profile with no art alongside an onboarded self-profile ⇒ still onboarded.
- [x] 13.5 Cover the layout short-circuit: an un-onboarded account renders the gate and not the requested page's content, no redirect is issued, and — per 9.6 — no page-level query runs.
- [x] 13.6 Cover the submit action: a new account is fully minted; an existing self-profile is renamed rather than duplicated; a blank name writes nothing; a submission that writes the profile but not the art leaves the account un-onboarded; re-submitting after that partial write succeeds with no duplicate profile.
- [x] 13.7 Cover cancel in both arms: the signup arm raises the confirmation and deletes nothing until confirmed, deletes `users` + `accounts` on confirmation, and returns to the gate on decline; the existing-account arm signs out and deletes nothing.
- [x] 13.8 Cover the gate's shell offering no route out: no close control renders, a click landing on the backdrop itself leaves the gate mounted, and Escape leaves it mounted. `FormShell` is untouched by this change beyond the requirement recording that a blocking surface renders its own shell, so it needs no new coverage.
- [x] 13.9 Cover `<ProfileAvatar>` across its three fills: art for a figurative style, the mask over the accent's ink for a glyph style, initials with no art. Assert no generic user icon renders in any case, and that no account image column is among its sources.
- [x] 13.10 Cover both host commit moments: on a creating host, confirming then dismissing without submitting writes nothing; on the editing host, confirming commits immediately and repaints; and cancelling restores the host's prior values on either. Cover too that the Settings form's own submit touches neither the accent nor the Altvatar row.
- [x] 13.11 Cover the identity action's partial-failure arm: either half of the write fails and the action reports failure rather than success, with a message that does not claim the identity was stored. Cover too that `updateProfileSettings` touches neither the accent nor the Altvatar row.
- [x] 13.12 Cover the update action's authorization for the art specifically: a `manager` submitting a changed name, accent **and** Altvatar is rejected with `Unauthorized` and writes no Altvatar row.
- [x] 13.13 Cover the birth form: submitting with a name and never opening the customizer creates a profile carrying both art and a stored accent; a failed art write still leaves the profile, its `owner` membership and a card rendering initials; the opening Altvatar and accent are not derived from the creating account or any existing profile.
- [x] 13.14 Cover the claims row and the list byline off account-linkage: a profile purchaser renders that profile's art whether or not an account backs it, a free-text purchaser renders initials from the entered name, and neither branches on account linkage.
- [x] 13.15 Cover the brand mark's accessible name as exactly `Altvatar`, with no part of its construction announced separately.
- [x] 13.16 Cover the bypass fix from 12.3: a non-default `BYPASS_SESSION_USER` resolves an actor rather than nothing.
- [x] 13.17 Write no unit tests for the migration or the seed script per the repo's test-scope bounds — the migration is covered by 1.5's applied check and the seed by 12.5.
- [x] 13.18 Cover the style roll: shuffle changes the style across rolls, never yields the glyph style, leaves every curated axis of the rolled style holding a value, and leaves the viewer on a control group that style has.

## 14. End-to-end

- [x] 14.1 Add a Playwright project and `webServer` entry per un-onboarded identity in `playwright.config.ts`, each with its own port and `BYPASS_SESSION_USER`. The bypass is process-global, so an identity is a server mode — that is a config addition, as the file's own comment says, not a redesign. Add the ids and ports to `e2e/helpers/constants.ts`.
- [x] 14.2 Add the onboarding-gate spec: for each identity, requesting a page inside the frame renders the gate and not that page's content.
- [x] 14.3 Assert the gate offers no route out: reload, backdrop click and Escape each leave it showing, and no close control is present.
- [x] 14.4 Assert cancel on the no-membership identity raises the deletion confirmation — and **decline it**. Submit nothing and confirm nothing: both are irreversible against a shared seeded database and would consume the fixture for every later run. Minting, atomicity and deletion are covered over the actions in section 13.
- [x] 14.5 Assert each spec touches only state its own server mode produced or the seed established, per the cross-process freshness rule.
- [x] 14.6 Document the flow's residue (none, by construction) in the spec file, and confirm re-running the suite against the same database exercises the flow identically.
- [x] 14.7 Ran the full suite against the reseeded database: 77 passed across all four projects. One existing spec did need repointing — `profiles.auth.spec.ts`'s accent-swatch flow reached for a picker that now lives inside the customizer, so it goes through `Edit Altvatar` → swatch → `Use this Altvatar`, selecting the label by the radio it wraps because the visible and screen-reader names repeat the preset name. The behaviour it covers is unchanged and still asserted. `profile-switch.auth.spec.ts` and `claim-attribution.auth.spec.ts` needed nothing.

## 15. Specs, docs and ADR

- [x] 15.1 **Re-opened:** `adr.md`'s Context was revised when `adventurer` was dropped, and the promoted copy still cites its `variant01`–`variant45` as the motivating example. Re-promote the revised text. The Decision and Consequences are unchanged, so `INDEX.md` and the term bank need nothing. Original task: promote `2026-08-26-generated-art-speaks-our-own-option-vocabulary` into `openspec/adr/2026-08-26-generated-art-speaks-our-own-option-vocabulary.md`, and add its row to `openspec/adr/INDEX.md` under **Touching** `Generated Art`. That term does not exist yet — add it to the term bank too, covering `lib/placeholderArt.ts` and `lib/altvatar/`.
- [x] 15.2 Promote `2026-08-26-onboarding-is-a-layout-short-circuit-not-a-guard` into `openspec/adr/2026-08-26-onboarding-is-a-layout-short-circuit-not-a-guard.md`, and add its rows to `openspec/adr/INDEX.md` under **Touching** `DAL` and `app/(main)/layout.tsx`.
- [x] 15.3 Put that entry's **Decision** into `app/(main)/layout.tsx` as a comment citing it — the layout renders onboarding instead of `children`, no per-action guard ships, and none is to be added. This is the WHY a future reader cannot recover from the code, which is the only comment CLAUDE.md licenses.
- [x] 15.4 Promote `2026-08-26-profile-art-never-comes-from-the-account` into `openspec/adr/2026-08-26-profile-art-never-comes-from-the-account.md`, and add its rows to `openspec/adr/INDEX.md` under **Touching** `DAL` and `DB Queries`.
- [x] 15.5 Promote `2026-08-26-customizable-art-stores-its-inputs-and-its-rendering` into `openspec/adr/2026-08-26-customizable-art-stores-its-inputs-and-its-rendering.md`, and add its rows to `openspec/adr/INDEX.md` under **Touching** `Generated Art` and `DB Schema`.
- [x] 15.6 Check each `MODIFIED` block in `specs/` against the source before applying it. Design records one known drift: `form-shell-system`'s class-composition requirement names `form-shell` / `form-shell-split` while the code renders `modal-shell form-shell` with no `split`. It is pre-existing and **not** fixed here — this delta modifies only the dismiss requirement. Flag it for `spec-hygiene` rather than repairing it inside a change that did not cause it.
- [x] 15.7 Refine `acceptance.md`'s flows with the literal handles the implementation landed — real control text, real routes, real command names — and confirm no `*TODO:*` marker survives. Refine, not rewrite: flow identity and journey scope stay as drafted.
- [x] 15.8 `DATABASE.md` needs no change: the table is additive with no backfill and no tightening pass, so it exercises the authoring workflow and the forward-only conventions the migration section already states rather than altering any of them.
- [x] 15.9 Run `openspec validate altvatar-and-onboarding-gate --strict` and resolve anything it reports.
- [x] 15.10 Promote `2026-08-27-patched-library-markup-is-re-verified-by-rendering` into `openspec/adr/2026-08-27-patched-library-markup-is-re-verified-by-rendering.md`, and add its row to `openspec/adr/INDEX.md` under **Touching** `Generated Art`.
- [x] 15.11 Correct the Reconnaissance line claiming core 10 has no published style packages. `@dicebear/styles@10.6.0` publishes all four. The section is non-binding but is the stated basis for the version choice, so it may not be left false.

## 16. Pre-merge

All five gates run locally against the author's real `.env.local` before review is
requested. This change edits production source, the schema, a migration, the seed,
the e2e config and tests, so no gate is exempt.

- [x] 16.1 `npm run lint` passes — zero errors, zero non-size warnings. `lib/data/profile.ts` and `profile.actions.ts` are already at the ceiling: confirm neither crossed 400, and that the new modules land green rather than yellow.
- [x] 16.2 `npx tsc --noEmit` passes — zero errors.
- [x] 16.3 `npm run build` passes — production build completes, and the per-style chunks split as 7.8 expects.
- [x] 16.4 `npm run test:coverage` passes — zero failing tests, every threshold met.
- [x] 16.5 `npm run test:e2e` passes — zero failing tests across every project, including the two new un-onboarded modes.

## 17. The move to DiceBear 10

Design D18. Taken rather than deferred: the mapping tables carry over almost
intact, so the migration is cheaper here than it would be as its own change, and
deferring would leave the repo carrying two DiceBear majors.

- [x] 17.1 Swap `@dicebear/core@9.4.3` and the five per-style packages for `@dicebear/core@10.7.0` + `@dicebear/styles@10.6.0`, both pinned exact. `@dicebear/shapes` goes with them — `lib/placeholderArt.ts` migrates rather than pinning the old major.
- [x] 17.2 Rewrite `lib/altvatar/render.ts` on `new Avatar(new Style(definition), options)`. Keep one dynamic import per style with the paths written out: a template literal defeats the bundler's static analysis and would pull all sixty published definitions into the chunk. Cache the `Style` instance — construction validates and deep-clones the definition, which is per-definition work, and the customizer re-renders on every click.
- [x] 17.3 Apply the `<component>Variant` suffix in `toNativeOptions`, the one place that writes native options. A binding's `native` stays the component's own name, so the mapping tables are untouched by the suffix.
- [x] 17.4 Rename the three components v10 renamed: `clothing`→`clothes` and `clothingGraphic`→`clothesGraphic` on avataaars, `body`→`clothes` on personas. Every variant name carries over unchanged — verified by checking each `native` and every mapped value against the shipped definitions, not by eye.
- [x] 17.5 Drop avataaars' `fixed: { style: ['default'] }`. v10 exposes no `style` option and its canvas draws no background disc, so the option that existed to avoid a doubled disc has nothing left to do.
- [x] 17.6 Give personas its `facialHairColor` axis, which v10 exposes and 9.x did not. It inherits from `hairColor` and hides on a clean-shaven face, exactly as avataaars' does, so a beard can diverge from the hair for the first time.
- [x] 17.7 Pin `animationVariant: ['none']` in `lib/placeholderArt.ts`. The `shapes` style gained an animation axis with no probability gate, so five of its six variants would set every newly minted item placeholder moving.
- [x] 17.8 Re-verify all three legibility rules **against rendered art**. Two matched nothing: avataaars moved from `#000` to `black`, and personas replaced its skin-derived features with a fixed literal it draws whatever the skin is. Fold personas and toon-head into one fixed-ink rule over a per-style ink list, since they now pose the identical problem, and keep personas' white-overlay rule alongside it — v10 renamed that literal from `#fff` to `white` but kept the wash.
- [x] 17.9 Add `flattenGlyph`. v10 stacks two copies of each glyph — a blended one that contributes nothing over a transparent background, and one at four-tenths opacity — and the disc's mask reads alpha, so the accent's ink would have painted at four-tenths.
- [x] 17.10 Update `scripts/altvatar-sheet.ts` onto the v10 API, reading axes off `Style.components()` and skipping aliases, which take no options of their own.
- [ ] 17.11 **Owner task**: re-walk the contact sheet after the bump. 3.11 was authored against 9.4.3's art and every rule that patches the library's markup had to be re-derived, so its pass does not carry over. This is the same eye 3.11 asks for, on the art v10 actually draws.

## 18. Gates — round 1

> Findings by durable ID (severity, `path:line`, citation, reconcile side) are in
> `review.md` Round 1. Resolve each open `Fix now` there before checking it off.

- [x] 18.1 A1+T2 Settings host persists on customizer confirm, not on the form's submit — resolved
- [x] 18.2 A3+B4 unset-accent fallback became the `iris` preset; five untouched assertions fail — resolved
- [x] 18.3 A5 partial-failure report no longer names which half saved — resolved
- [x] 18.4 A6 tasks 5.1–5.3 marked complete for a `FormShell` prop that does not exist — resolved
- [x] 18.5 A7 `FormShell`'s `header` prop is documented by no task and no delta — resolved
- [x] 18.6 A8 `role="dialog"` on the gate and the customizer, banned by design's Non-Goals — resolved
- [x] 18.7 A9 birth form reshaped (title, footer, brand band) beyond the disc swap — resolved
- [x] 18.8 A10 `UserCard` redesigned and its `compact` prop deleted; `ListCard` accent-tinted — resolved
- [ ] 18.9 A11 owner tasks 3.11, 12.5 and 17.11 still unchecked — resolved
- [x] 18.10 B12 cached reads join avatar art and accent with no narrow tag for either — resolved
- [x] 18.11 B13 hand-rolled radiogroups bypass `segmented-control-system` and its keyboard contract — resolved — _no work: dropped at adjudication, the customizer is its own primitive family. See Round 1 `### Adjudications` in review.md._
- [x] 18.12 B14+C15 dead `users/ui/styles/avatar.css` still imported by the layout — resolved
- [x] 18.13 B16+C17 three caching rationales name a join and an event this change removed — resolved
- [x] 18.14 B18 `DATABASE.md`'s worked example points at a function that moved — resolved
- [x] 18.15 B19 `scripts/test-e2e.sh` header still says two servers — resolved
- [x] 18.16 B20 WCAG relative-luminance derivation written three times — resolved
- [x] 18.17 B21 `ProfileAvatar` imports `initialsOf` from a route-group feature directory — resolved
- [x] 18.18 C22 `abandonAccount`'s guard is narrower than the invariant it asserts — resolved — _no work: dropped at adjudication, the chain is self-inflicted and unreachable from the UI. See Round 1 `### Adjudications` in review.md._
- [x] 18.19 C23 `createProfile` authorizes on `authedUserId()`, bypassing the onboarding gate's ADR — resolved
- [x] 18.20 C24 `accent.ts` comment states the opposite of the code beneath it — resolved
- [x] 18.21 C25 `accentShadow` left on the brand fallback while its four siblings moved — resolved
- [x] 18.22 C26 commented-out CSS shipped in three places in `altvatar.css` — resolved
- [x] 18.23 C27 `options` jsonb column lacks `$type<>`, forcing an unchecked cast — resolved
- [x] 18.24 C28 `onboarding.css` writes `#000` where `--dark-color` holds that role — resolved
- [x] 18.25 T29 `seedUserEmail` and the synthesized session's email are unpinned — resolved
- [x] 18.26 T30 `profile_avatars` primary key and cascade have no test — resolved
- [x] 18.27 T31 `ProfileHeaderSection` mock still types the removed account `image` prop — resolved
- [x] 18.28 T32 `NoStoredAltvatar_RollsOneWithoutWriting` constrains neither half of its name — resolved
- [x] 18.29 T33 `NoAccent_StillRendersItsFillRatherThanNothing` never reads the fill — resolved
- [x] 18.30 T34 neither host asserts the absence of an accent field on the closed form — resolved
- [x] 18.31 T35 nothing asserts the NextAuth config now wires no `createUser` event — resolved
- [x] 18.32 `npm run lint` — 0 errors, 0 non-size warnings (2 tolerated `sonarjs/max-lines` yellows: `profile.actions.ts` 366, `profile.ts` 362)
- [x] 18.33 `npx tsc --noEmit` — 0 errors
- [x] 18.34 `npm run build` — completes successfully
- [x] 18.35 `npm run test:coverage` — 274 files, 3420 tests, all passing; statements 99.89%, branches 99.03%, functions 100%, lines 99.95%
- [x] 18.36 `npm run test:e2e` — 77 passed across the four projects

## 19. Gates — round 2

> Findings by durable ID (severity, `path:line`, citation, reconcile side) are in
> `review.md` Round 2. Resolve each open `Fix now` there before checking it off.

- [ ] 19.1 A11 owner tasks 3.11, 12.5 and 17.11 still unchecked — resolved
- [ ] 19.2 A1 `form-shell-system` MODIFIED block restates canonical text the code already contradicts — resolved
- [ ] 19.3 A2 `design.md`'s Context bullet still carries the unbounded guarantee the ADR now disclaims — resolved
- [ ] 19.4 A3 fallback scenario says "all four derivations" where there are five — resolved
- [ ] 19.5 A4 task 4.8 cites two acceptance flow titles the fix renamed — resolved
- [ ] 19.6 B5 CLAUDE.md's worked example names a `utils.ts` path that no longer exists — resolved
- [ ] 19.7 B6 LOCALDEV.md's accent-coverage paragraph contradicts itself and the seed — resolved
- [ ] 19.8 B7 the e2e swatch test now writes rows its RESIDUE block does not list — resolved
- [ ] 19.9 B8 `lib/data/user.ts` comment names the viewer's row where the join reaches the followee's — resolved
- [ ] 19.10 C10 two `Not cached:` comments narrate the delta's own history — resolved
- [ ] 19.11 T11 `FormShell`'s `header` scenario is pinned by no test at the primitive — resolved
- [ ] 19.12 T12 `AltvatarPreview` has no test; accent-without-regeneration is unpinned — resolved
- [ ] 19.13 T13 `withoutOverlaysOver`'s wiring into the tiles is unpinned — resolved
- [ ] 19.14 T14 the nav trigger's art leg has no case — resolved
- [ ] 19.15 T15 both rail tests moved from `toEqual` to `toMatchObject` — resolved
- [ ] 19.16 `npm run lint` — zero errors, zero non-size warnings
- [ ] 19.17 `npx tsc --noEmit` — zero errors
- [ ] 19.18 `npm run build` — completes successfully
- [ ] 19.19 `npm run test:coverage` — run result
- [ ] 19.20 `npm run test:e2e` — run result
