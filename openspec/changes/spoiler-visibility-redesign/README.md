# spoiler-visibility-redesign

Replace the binary owner-only spoiler toggle with per-viewer, membership-keyed spoiler visibility: a single four-stage tier (surprise → progress → claims → identity) set from the list hero, owner-set per-member baselines stored on `profile_preferences`, and a transient per-list override.
