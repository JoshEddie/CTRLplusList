## 1. Test scaffolding (jsdom render + RSC harness)

- [x] 1.1 Reuse the existing jsdom setup (`test/helpers/setup.ts`) and `@testing-library/user-event`. Establish the per-surface mock kits: an async-RSC kit (`vi.mock('@/lib/auth')`, `vi.mock('@/lib/dal')` stubbing the read(s) each shell calls, `vi.mock('next/navigation')` with `redirect()` and `notFound()` each throwing a distinct sentinel via `vi.hoisted` — mirror `FollowingPage.test.tsx`'s `REDIRECT:${url}` pattern, extended with `NOTFOUND`); a layout-shell kit (mock the async child section/page, render the shell, assert `Suspense` fallback + composition); a pure-render kit (`vi.mock('next/link')` → plain `<a>`); and a client kit (`vi.mock('@/app/actions/follows')` → the four mutation spies, `vi.mock('react-hot-toast')` → `toast.success`/`error` spies, `vi.mock('next/navigation')` → `useRouter` `{ refresh }` spy).
- [x] 1.2 Provide the shared Arrange factories: `makeSession({ email, name, image })` (the `next-auth` `Session` shape the sections `await auth()` into), `makeViewer({ id })` (the `getUserIdByEmail` row), and `makeProfile({ id, name, image, publicListCount, viewerIsBlocked, blockedByViewer, viewerIsFollowing })` (the `getProfileForUser` shape). Pass `params`/`searchParams` as `Promise.resolve({ … })` (the App-Router async-props shape the shells `await`). Hoist shared Arrange (mock defaults, `vi.clearAllMocks()`) into each file's `beforeEach`; extract a colocated `__tests__/test-helpers.tsx` **per directory** only if the duplication audit finds 3+ reuse (per TESTING.md) — likely in `settings/connections/__tests__/` (the three sections share the session/viewer/sentinel Arrange).

## 2. Async RSC shell tests

- [x] 2.1 `settings/connections/__tests__/FollowingSection.test.tsx` — `auth()` → `null` throws the `REDIRECT:/` sentinel; a session whose `getUserIdByEmail` → `null` throws the `REDIRECT:/` sentinel; a resolved viewer + a stubbed `getFollowingByUser` renders the real `ConnectionsSection` ("Following (N)") with one `ConnectionRow` per followee (name → `/user/${followee_id}` link, since-date) and a single **Unfollow** `ConnectionsAction` per row. Covers the `count === 0` empty branch ("Not following anyone yet.").
- [x] 2.2 `settings/connections/__tests__/FollowersSection.test.tsx` — same two redirect legs; a resolved viewer + a stubbed `getFollowersOfUser` renders "Followers (N)" with **Remove** + **Block** actions per row, and the empty branch ("No followers yet.").
- [x] 2.3 `settings/connections/__tests__/BlockedSection.test.tsx` — same two redirect legs; a resolved viewer + a stubbed `getBlockedByUser` renders "Blocked (N)" with a single **Unblock** action per row, and the empty branch ("No blocked users."). (LOCKS the `following` "Owners SHALL view and manage their followers" three-section structure across §2.1–§2.3.)
- [x] 2.4 `user/[id]/__tests__/ProfileHeaderSection.test.tsx` — the substance core (Decision 4): `getProfileForUser` → `null` throws the `NOTFOUND` sentinel; `viewerIsBlocked: true` throws the **same** `NOTFOUND` sentinel (LOCKS the block cover-story — block and absent are indistinguishable); a reachable non-follower with `searchParams.follow === '1'` renders the (mocked) `FollowPrompt` with the profile `name`, while `?follow` absent / `viewerIsFollowing: true` / self (`viewerId === id`) / `blockedByViewer: true` renders no prompt; `ProfileHeader` (mocked) always receives `{ user, publicListCount, viewerId, showFollowButton: isReachable }`. Covers the session-less path (`auth()` → `null` ⇒ `viewerId = null`, `getUserIdByEmail` not called). LOCKS the `following` "Blocks SHALL gate URL access" + "Profile pages" header behaviors.
- [x] 2.5 `user/[id]/__tests__/ProfileListsSection.test.tsx` — awaits `params`, calls `getPublicListsByUser(id, { limit: 50 })` (assert the args), and forwards the resolved lists to the (mocked) `PublicListsGrid`. Covers the empty-lists pass-through (`PublicListsGrid` receives `[]`).

## 3. Pure layout shell tests

- [x] 3.1 `settings/connections/__tests__/page.test.tsx` — the default `Page` export renders `<ConnectionsPage />` (mock `./ConnectionsPage`); assert the exported `metadata.title === 'Connections'`.
- [x] 3.2 `settings/connections/__tests__/ConnectionsPage.test.tsx` — renders the real `Header` ("Connections") + three `Suspense`-wrapped sections (mock `./FollowingSection` / `./FollowersSection` / `./BlockedSection`) with `LoadingIndicator size="rail"` fallbacks and two `role="separator"` dividers in order. Assert the composition + ordering (Following → Followers → Blocked).
- [x] 3.3 `user/[id]/__tests__/page.test.tsx` — the default `Page` export renders `<ProfilePage {...props} />` forwarding `params`/`searchParams` (mock `./ProfilePage`, assert the forwarded props); assert `metadata.title === 'User profile'`.
- [x] 3.4 `user/[id]/__tests__/ProfilePage.test.tsx` — renders the real `ListCollectionsNav` + `Header` ("Lists") + two `Suspense`-wrapped sections (mock `./ProfileHeaderSection` / `./ProfileListsSection`); assert `ProfileHeaderSection` receives `params`+`searchParams` and `ProfileListsSection` receives `params`, with `LoadingIndicator size="rail"` then `size="page"` fallbacks.

## 4. Pure render component tests

- [x] 4.1 `settings/connections/__tests__/ConnectionsSection.test.tsx` — `count > 0` renders the `title (count)` heading + the `<ul className="connections-list">` children; `count === 0` renders the `<p className="connections-empty">` `emptyMessage` and no `<ul>`. (Both branches of the count ternary.)
- [x] 4.2 `settings/connections/__tests__/ConnectionRow.test.tsx` — renders the `/user/${userId}` `Link` (mocked `next/link` → `<a href>`); a present `name` renders it, an absent `name` renders the `'Unnamed'` fallback; `since` present renders the `formatDate` subline (assert the localized short date, e.g. "May 19, 2026"), `since` absent renders no subline; the `actions` slot renders its children. (LOCKS the `following` "since date" + the full-name display clauses.)

## 5. Client component test

- [x] 5.1 `settings/connections/__tests__/ConnectionsActions.test.tsx` — for each `action` (`unfollow` / `remove` / `block` / `unblock`) the `Button` renders the right label; clicking dispatches `fns[action](targetId)` (assert the correct `@/app/actions/follows` spy called with `targetId`); the success branch (`{ success: true, message }`) calls `toast.success(message)` + `router.refresh()`; the error branch (`{ success: false, message }`) calls `toast.error(message)` and **not** `router.refresh()`; the `isPending` re-entrancy guard (`aria-disabled` set during the transition; a click while pending early-returns without a second dispatch). Renders through the real `Button`/`buttonClasses` (3.1, not re-owned). Drive with `@testing-library/user-event`.

## 6. Audit findings (recorded BEFORE coverage validation)

- [x] 6.1 **Dead-code audit** (Decision 2) — confirm all 12 files are live and record the importer chain: `settings/connections/page.tsx` + `user/[id]/page.tsx` are Next App-Router entry points; `ConnectionsPage`/`ProfilePage` ← their `page.tsx`; the sections ← those; `ConnectionsSection`/`ConnectionRow`/`ConnectionsActions` ← the sections. **No deletion** (contrast 9.3). Confirm `tsc --noEmit` + `npm run build` stay green.
  - **Finding:** all 12 files confirmed live, importer chain as stated (verified by reading the source imports). No deletions. `tsc --noEmit` clean and `npm run build` exits 0 ("Compiled successfully", page data collected).
- [x] 6.2 **Route-path drift finding** (Decision 7) — record that the active `following` "Profile pages SHALL exist at `/u/[id]`" requirement (title, prose, four scenarios) names `/u/[id]`, but the live route is `app/(main)/user/[id]` (→ `/user/[id]`), and the same spec's "Owner name linkified" requirement + `ConnectionRow`'s `Link` both use `/user/{id}`. Internal spec inconsistency / stale prose. Recommended operator disposition: reconcile `/u/[id]` → `/user/[id]` in the `following` spec at the §7 close-out. **Not modified here** (cross-tier, test-only carve-out); the new tests pin the real `/user/${id}` target.
  - **Finding:** confirmed — the live route is `app/(main)/user/[id]` and `ConnectionRow` links `/user/${userId}`; the new `ConnectionRow` / section tests assert the `/user/${id}` href. The stale `/u/[id]` prose in the active `following` spec is left for the operator to reconcile at the §7 close-out (not edited here — cross-tier).
- [x] 6.3 **Duplication audit** — hoist shared Arrange into each file's `beforeEach`; extract a colocated `__tests__/test-helpers.tsx` per directory only on 3+ reuse (per TESTING.md). Record the disposition (expect a `settings/connections/__tests__/test-helpers.tsx` carrying `makeSession`/`makeViewer`/the sentinels/the `next/link` mock).
  - **Finding:** extracted `settings/connections/__tests__/test-helpers.tsx` carrying `makeSession` / `makeViewer` / the `redirectMock` sentinel (3+ reuse across the three section tests). The **`next/link` mock was NOT reduplicated** — the canonical `MockNextLink` already lives in `app/ui/components/__tests__/test-helpers` (already imported cross-directory by the `users/ui` tests), so all four connections files that need it (`ConnectionRow` + the three sections) reuse that one per DRY. The `user/[id]/` directory needed no `test-helpers.tsx`: `makeProfile` / session Arrange is used by `ProfileHeaderSection` alone (1 file → inline `beforeEach`); its `next/link` need (`ProfilePage`) also reuses the canonical `MockNextLink`.
- [x] 6.4 **Complexity audit** — measure `sonarjs/cognitive-complexity` on the 12 covered files at HEAD; any function ≥ 15 (only `ProfileHeaderSection.tsx` is a candidate) is disposed by a single-file, behavior-preserving extraction covered by the new tests — never by raising the ceiling. Record each disposition (expect: no extraction needed).
  - **Finding:** no extraction needed. With `sonarjs/cognitive-complexity` promoted to `error` (15) for all 12 files, `eslint .` reports 0 errors (the only complexity warning remaining is the untouched `seed-dev-users.ts`). `ProfileHeaderSection.tsx` — the only branching candidate — is well under the ceiling. No source modified.
- [x] 6.5 **Assertion audit** — every new test asserts an observable (exact rendered text/structure, the `redirect`/`notFound` sentinel, a forwarded prop, a `Link` `href`, a count-branch class, a dispatched action spy, the toast/refresh branch); no tautologies, execute-for-coverage, or snapshot-only tests. Record the §6.4 result.
  - **Finding:** all 48 new tests assert observables — thrown `REDIRECT:/` / `NOTFOUND` sentinels, exact heading/empty-message text, `getAllByRole('listitem')` counts, `/user/${id}` link hrefs, forwarded-prop `data-*` attributes (ProfileHeader / FollowPrompt / sections), the `getProfileForUser(id, viewerId)` / `getPublicListsByUser(id, { limit: 50 })` call args, the dispatched `@/app/actions/follows` spies, and the toast-success+refresh vs toast-error+no-refresh branches. No tautologies, execute-for-coverage, or snapshot-only tests. `vitest/expect-expect` and `vitest/valid-title` pass at `error`.
- [x] 6.6 **Invariant elevation** (Decision 6) — record the **no-elevation** decision with rationale: the three connections sections + actions + since-date are already `following` SHALLs (LOCKED); the profile render + invite-prompt + unknown-user 404 are already a `following` SHALL (LOCKED); the **block cover-story `notFound()`** is already the `following` "Signed-in blocked user 404s on profile page" SHALL (LOCKED via the identical-sentinel assertion, §2.4); the section auth-guards and the `ConnectionsActions` success/error branches are small guards with no owning capability — unit-covered, not elevated. No new requirement authored.

## 7. Enumeration + complexity promotion

- [x] 7.1 `vitest.config.ts`: ADD the 12 covered files to per-file `thresholds` at `COVERAGE_FLOOR` under a `// test-connections-and-profile-pages (sub-proposal 9.4) — locked at universal COVERAGE_FLOOR.` comment.
- [x] 7.2 `eslint.config.mjs`: ADD the same 12 paths to the per-file `sonarjs/cognitive-complexity = error` array under the matching comment. `eslint .` reports no new issues (0 errors; only the pre-existing `Avatar.tsx` / `seed-dev-users.ts` warnings remain).
  - **Result:** `eslint .` → 0 errors, 2 warnings (`Avatar.tsx` no-img-element + `seed-dev-users.ts` cognitive-complexity — both pre-existing, untouched).

## 8. Coverage validation

- [x] 8.1 **Acceptance criterion** — with the §7.1 `thresholds` added, `npm run test:coverage` passes the `perFile` gate with zero "does not meet" errors: every covered file at `lines ≥ 98 / statements ≥ 98 / branches ≥ 95 / functions = 100`, with zero `/* v8 ignore */` added. Record the test-file / test counts (expect net **+12 jsdom test files**, plus any per-directory `test-helpers.tsx`).
  - **Result:** `npm run test:coverage` exits 0 with zero "does not meet" errors. All 12 covered files measure **100/100/100/100** (lines/statements/branches/functions) — comfortably above the floor; **zero `/* v8 ignore */` added**. Counts: **184 test files / 2048 tests, all passing** (net **+12 jsdom test files** plus one `settings/connections/__tests__/test-helpers.tsx` fixture).

## 9. Specs + governance bookkeeping

- [x] 9.1 `testing-foundation` delta (`specs/testing-foundation/spec.md`) — Tier-2 carve-out bookkeeping (the 12 page-shell files whole-covered + enumerated); archive-only per design D13. No apply-time write to the active `testing-foundation` spec or the parent `test-coverage` accumulator. `openspec validate test-connections-and-profile-pages --strict` passes.
  - **Result:** `openspec validate test-connections-and-profile-pages --strict` → valid. No apply-time write to the active `testing-foundation` spec or the `test-coverage` accumulator (archive-only per D13).
- [x] 9.2 Confirm NO active capability spec changes at apply-time: the `following` SHALLs are LOCKED as-is (asserted by the new tests, not modified); the `/u/[id]` route-path drift is flagged for the operator (§6.2), not edited.
  - **Result:** confirmed — no `following` (or other active capability) spec edited; the only repo changes are the 12 `.test.tsx` files + one `test-helpers.tsx`, the `vitest.config.ts` thresholds, and the `eslint.config.mjs` complexity-error entries. No source files modified.
- [ ] 9.3 **On archive** (at `/opsx:archive`): flip `test-coverage/tasks.md` §9.4 to `[x]` with a landed/archived note. §7.2 / §7.3 (global complexity promotion + final coverage baseline) remain blocked while §9.5 is still open — this sub-proposal alone does not clear them. *(Deferred to `/opsx:archive`; not part of apply.)*

## 10. Pre-merge (four-gate)

- [x] 10.1 `npm run lint` passes — 0 errors, no new warnings (the pre-existing `Avatar.tsx` img-element + `seed-dev-users.ts` complexity warnings are untouched). → `eslint .`: 0 errors, 2 pre-existing warnings.
- [x] 10.2 `npx tsc --noEmit` passes with zero errors. → "No errors found".
- [x] 10.3 `npm run test` (full suite) passes — all green including the +12 new files. → 184 files / 2048 tests, all pass.
- [x] 10.4 `npm run build` completes successfully (with DB env present; the bare-shell failure is the environmental missing-`DATABASE_URL` at page-data collection, not a code defect — compilation + type validation pass either way). → exit 0, "Compiled successfully", page data collected.
