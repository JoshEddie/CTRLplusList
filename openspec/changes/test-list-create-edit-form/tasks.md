## 1. Test scaffolding (jsdom render + RSC harness)

- [x] 1.1 Reuse the existing jsdom setup (`test/helpers/setup.ts`) and `@testing-library/user-event`. Establish the per-surface mock kits: a client kit (`vi.mock('@/app/actions/lists')`, `vi.mock('next/navigation')` → `{ push, refresh }` spies, `vi.mock('react-hot-toast')`) and an async-RSC kit (`vi.mock('@/lib/auth')`, `vi.mock('@/lib/dal')`, `vi.mock('next/navigation')` with `redirect()` throwing a sentinel — mirror `FollowingPage.test.tsx` / `ChooseItemsBody.test.tsx`).
  - **Disposition:** mock kits are inline per file (vi.mock hoists per module). `react-hot-toast` is mocked via its **default** export (`{ default: { success, error } }`) for `VisibilityPicker` / `DeleteListButton` (default imports). The async-RSC `redirect()` sentinel mirrors `ChooseItemsBody.test.tsx` / `ListItemsSection.test.tsx`.
- [x] 1.2 Provide a `ListTable` fixture builder (reuse any existing builder under `app/actions/__tests__/test-helpers.ts` / `app/(main)/lists/ui/components/__tests__/test-helpers.ts`; extract a colocated one only if the duplication audit finds 3+ reuse). Cover the fields `ListForm`/`EditListBody`/`ListHeroSection` read: `id`, `name`, `subtitle`, `occasion`, `date`, `user_id`, `visibility`, `items`.
  - **Disposition:** reused the existing colocated `makeList` (`app/(main)/lists/ui/components/__tests__/test-helpers.ts`) for the form/container tests — it covers `id`/`name`/`subtitle`/`occasion`/`date`/`user_id`(+optional `visibility`). No new builder extracted (no 3+ reuse beyond it). The cross-directory RSC tests (`EditListBody`, `ListHeroSection`) build inline `as never` fixtures per the sibling RSC precedent (`ListItemsSection.test.tsx` / `ChooseItemsBody.test.tsx`), which is also how `items`/`visibility` are supplied — `makeList` was not modified.
- [x] 1.3 Provide the `next/server` `after`-capture helper for `ListHeroSection` (Decision 4): `vi.mock('next/server', …)` pushing each `after` callback into a captured array the test invokes explicitly. Keep it local to the `ListHeroSection` test unless a 3rd consumer appears.
  - **Disposition:** kept local to `ListHeroSection.test.tsx` (a 3-line `vi.hoisted` array + `vi.mock('next/server')`). No other covered file defers via `after()`, so no extraction.

## 2. Client component tests (`app/(main)/lists/ui/components/__tests__/`)

- [x] 2.1 `ListForm.test.tsx` — create mode: a valid submit calls `createList` with the normalized `{ name, subtitle, occasion, date }` payload and on `{ success: true, id }` navigates to `/lists/{id}/choose-items?new=1`. Assert the **subtitle trim-to-`null`** (whitespace-only subtitle → `subtitle: null` in the payload) — LOCKS the existing `list-metadata` normalization requirement.
- [x] 2.2 `ListForm.test.tsx` — edit-as-modal (`onClose` provided): a valid submit calls `updateList(list.id, payload)` and on success calls `onSuccess` → `onClose` → `router.refresh()`, with NO `router.push`. Edit-as-page (no `onClose`): on success navigates to `/lists/{id}`. (Order locked via `mock.invocationCallOrder`.)
- [x] 2.3 `ListForm.test.tsx` — failure + validation: a `{ success: false, message }` result keeps the form mounted and renders `message` with no navigation; an unparseable date blocks dispatch (no `createList`/`updateList` call) and surfaces the date field error. Assert the literal `validateDate` messages as shipped (the year-bound copy mismatch is recorded in §5, not changed here — Decision 8).
  - **Note:** the action's date guard is exercised via `fireEvent.submit` (bypasses jsdom constraint validation so the empty-date path reaches the action); the thrown-error `catch` is exercised with `mockRejectedValue` (both `err.message` and the `'An error occurred'` fallback).
- [x] 2.4 `ListForm.test.tsx` — render details: `isEditing` renders the `DeleteListButton` footer slot and the "Update List" submit label; create renders "Create List" and no delete slot; pending state disables the fields. Renders through the **real** `FormShell` / field primitives (not mocked).
- [x] 2.5 `VisibilityPicker.test.tsx` — optimistic apply: selecting a different row calls `setListVisibility(listId, next)`, optimistically shows the new label, and on `{ success: true }` shows `rowFor(next).toast` + calls `router.refresh()`. LOCKS the new `list-visibility` optimistic-apply requirement.
- [x] 2.6 `VisibilityPicker.test.tsx` — rollback + no-op: on `{ success: false, message }` the pill rolls back to the prior value, `toast.error(message)` fires, and `router.refresh()` is NOT called; re-selecting the current row makes no `setListVisibility` call (LOCKS the existing no-op early-return); pending disables the rows. Renders through the **real** `Menu`/`MenuItemRadio`/`PopoverTrigger`. (Escape-dismiss covers the `Menu onClose` path.)
- [x] 2.7 `DeleteListButton.test.tsx` — the `ConfirmDialog`-gated flow: clicking Delete opens the dialog; confirming calls `deleteList(id)` and on success toasts + `router.push('/lists')`; on `{ success: false, error }` toasts the error and does not navigate; cancel closes without calling `deleteList`. Renders through the real `ConfirmDialog` / `Button`. (Both the `error` and the `'Failed to delete list'` fallback are covered.)
- [x] 2.8 `NewListButton.test.tsx` — closed by default; clicking opens `ListFormContainer`; the `variant`/`label` props flow to the trigger; the `mobile-hide` label span renders. `ListFormContainer.test.tsx` — passes `list`/`isEditing`/`onClose`/`onSuccess` straight through to `ListForm` (mock `ListForm`, assert the forwarded props).

## 3. Pure render component tests (`app/(main)/lists/ui/components/__tests__/`)

- [x] 3.1 `ListPrivate.test.tsx` — renders the "This list is private" interstitial; the `!loggedIn` branch renders the extra "please login to view it" paragraph and the `loggedIn` branch omits it. LOCKS the existing `list-visibility` private-list-interstitial requirement at the component level.
- [x] 3.2 `EmptyList.test.tsx` — renders "No lists found" + the "Create List" `LinkButton` pointing at `/lists/new`.

## 4. Async RSC shell tests

- [x] 4.1 `app/(main)/lists/new/__tests__/page.test.tsx` — unauthenticated (`auth()` → null) → `redirect('/')` sentinel; authenticated but `getUserIdByEmail` → null → `redirect('/')`; authenticated owner → renders `<ListForm />` (create mode, no `list`). `loading.test.tsx` — renders the loading shell (Header "Loading..." + `LoadingIndicator size="page"`, as shipped).
- [x] 4.2 `app/(main)/lists/[id]/edit/__tests__/EditListBody.test.tsx` — same guard matrix (no session → redirect; no user → redirect); authed → calls `getList(id)` and renders `<ListForm list isEditing />`. `page.test.tsx` — renders `EditListBody` inside `<Suspense>` with the `LoadingIndicator size="form"` fallback (mock `EditListBody`).
- [x] 4.3 `app/(main)/lists/[id]/__tests__/ListHeroSection.test.tsx` — guard + projection: a non-owner of a `VISIBILITY.OWNER` list renders `<ListPrivate loggedIn>`; otherwise renders `<ListDetails>` (mock `ListDetails`, floored under 4.7) and assert the forwarded `isOwner` / `showSpoilers` (owner + `spoilers=1`) / `previewMode` (owner + `preview=viewer`) / `itemCount` props derive correctly. Mock `guardListViewable`, `getList`, `getUserById`, `getUserIdByEmail`, `auth()`.
- [x] 4.4 `ListHeroSection.test.tsx` — **visit-recording (the high-value gap)**: using the `after`-capture helper (§1.3) and a `@/db` insert-chain spy (Decision 4b), assert the upsert is performed for an authenticated non-owner of a non-private list with the correct `(user_id, list_id, last_visited_at, visit_count)` values + `onConflictDoUpdate` target/set shape; and assert NO upsert for the owner, for an unauthenticated viewer, and for a non-owner of a Hidden list. Asserts against the existing `visit-history` "authenticated non-owned non-private" SHALL — replaces 4.14's mirror. (The `after()` `catch` log path is also covered.)
- [x] 4.5 `app/(main)/lists/[id]/__tests__/page.test.tsx` (Decision 2) — the `ListPage` default export renders both `<Suspense>` sections (`ListHeroSection` + `ListItemsSection`) with the right fallback sizes (`rail` / `page`; mock both sections). The existing `page.generateMetadata.test.ts` stays untouched. `loading.test.tsx` — renders the page-size loading shell.

## 5. Dead-code disposition + audit findings (recorded BEFORE coverage validation)

- [x] 5.1 **Dead-code audit** — DELETE `app/(main)/lists/ui/components/ListHeader.tsx` (zero importers repo-wide; Decision 3). Confirm `tsc --noEmit` + `npm run build` stay green after removal. Record the `ReorderInputGroup.tsx` importer-less observation as a §7-close-out follow-up (NOT removed here — out of this carve-out's boundary).
  - **Finding:** `ListHeader.tsx` had zero importers under `app/`/`lib/`/`e2e/`/`test/` (grep confirmed) — deleted. `tsc --noEmit` and `npm run build` are green post-removal. `ReorderInputGroup.tsx` is **also** importer-less (confirmed by the same grep) but sits in item-management UI outside this carve-out — flagged for the §7 close-out audit, not removed here.
- [x] 5.2 **Non-existent file resolution** — record that `EditListInputGroup` (named in issue #112) does not exist in the codebase; the edit flow is covered by `ListForm` (`isEditing`) + `EditListBody` + the already-floored `EditListAction`. No file to test.
  - **Finding:** repo-wide grep finds no `EditListInputGroup` file. The list edit flow is fully covered by `ListForm` (the `isEditing` branch), `EditListBody` (edit page shell), and `EditListAction` (already floored under 4.7). Treated as a stale name in the issue text; no file to test.
- [x] 5.3 **Duplication audit** — hoist shared Arrange into `beforeEach`; extract a colocated `__tests__/test-helpers.ts` fixture only on 3+ reuse (per TESTING.md). Record the disposition.
  - **Finding:** shared per-file Arrange (mock defaults, `vi.clearAllMocks()`) hoisted into each file's `beforeEach`. The `makeList` fixture is reused from the existing colocated `test-helpers.ts`; no new fixture extracted (the `after`-capture helper and the `@/db` insert spy are single-consumer, kept local in `ListHeroSection.test.tsx`).
- [x] 5.4 **Complexity audit** — measure `sonarjs/cognitive-complexity` on every covered file at HEAD; any function ≥ 15 (candidates: `ListForm`, `ListHeroSection`) is disposed by a single-file, behavior-preserving extraction covered by the new tests — never by raising the ceiling. Record each disposition.
  - **Finding:** `npx eslint` with `sonarjs/cognitive-complexity` promoted to `error` (ceiling 15) reports **no issues** across all 14 covered files — `ListForm.tsx` and `ListHeroSection.tsx` both measure under 15 at HEAD. No extraction needed; the ceiling was not raised.
- [x] 5.5 **Assertion audit** — every new test asserts an observable behavior (exact action payload, navigation target, toast text, rendered text/branch, or upsert shape); no tautologies, execute-for-coverage, or snapshot-only tests. Record the `ListForm.validateDate` copy-vs-guard mismatch (rejects `< 1000`, message says "1900 or later") as an operator-decision finding (tighten guard to 1900 OR soften copy) — locked as-shipped by the tests, not changed here (Decision 8).
  - **Finding:** every new test constrains an observable: the exact `createList`/`updateList`/`deleteList`/`setListVisibility` payload, the `router.push`/`refresh` target, the toast string, the rendered branch text, or the `list_visits` upsert shape. No execute-for-coverage / tautology / snapshot-only tests.
  - **Operator-decision finding (Decision 8):** `ListForm.validateDate` rejects `date.getFullYear() < 1000` but its message reads "Please enter a year of 1900 or later" — guard and copy disagree (a 1500 date passes the guard yet the copy implies otherwise). The tests **lock the shipped behavior** (reject `< 1000`, accept `≥ 1000`) and assert the literal strings. Recommended disposition for the operator: either tighten the guard to `< 1900` or soften the copy to "1000 or later". Not changed here — a validation-bound change is outside this test-only carve-out.
- [x] 5.6 **Invariant elevation** — ELEVATE the two UI invariants: the form create-vs-edit dispatch+navigation contract to `list-metadata` (Decision 6) and the `VisibilityPicker` optimistic-apply/rollback contract to `list-visibility` (Decision 7). Record NON-elevation of the `ListHeroSection` visit guard (already a `visit-history` SHALL — tested against it, not re-specced) and the private-list interstitial (already a `list-visibility` SHALL).
  - **Finding:** two ADDED requirements authored as deltas — the form mode-dispatch+navigation contract (`specs/list-metadata/spec.md`) and the `VisibilityPicker` optimistic-apply/rollback contract (`specs/list-visibility/spec.md`). **Non-elevated:** the `ListHeroSection` visit-recording guard (already covered by the `visit-history` "authenticated non-owned non-private SHALL be recorded" requirement — tested against it, no new SHALL) and the private-list interstitial (already a `list-visibility` SHALL — locked at the component level only).

## 6. Enumeration + complexity promotion

- [x] 6.1 `vitest.config.ts`: add every covered file to per-file `thresholds` at `COVERAGE_FLOOR` under a `// test-list-create-edit-form (sub-proposal 9.2) — locked at universal COVERAGE_FLOOR.` comment (do NOT enumerate the deleted `ListHeader.tsx`).
- [x] 6.2 `eslint.config.mjs`: add the same paths to the per-file `sonarjs/cognitive-complexity = error` array under the matching comment. `npx eslint <files>` reports no issues after any §5.4 extraction.
- [x] 6.3 `eslint.config.mjs`: promote `@typescript-eslint/no-unused-vars` from the Next preset's `warn` to a **global `error`** with **no** `^_` ignore pattern (an exemption is just the lazy fix for a red build — rename `x` to `_x` — and is worse than the warning because it looks enforced; the `args: after-used` default still permits a genuinely-needed leading positional parameter). This surfaced three dead `vi.fn` spy params (`_config`/`_row`/`_table`) in `ListHeroSection.test.tsx`'s `dbSpy`; disposed by **removal**, not underscore-silencing — the call-arg types moved onto the `vi.fn<…>()` generics, preserving the `mock.calls[0][0]` type-safety with zero unused bindings.
  - **Result:** `npx eslint .` → 0 errors (the 2 unrelated pre-existing warnings remain — `Avatar.tsx` img-element, `seed-dev-users.ts` complexity); `npx tsc --noEmit` clean; `ListHeroSection.test.tsx` 10/10.

## 7. Coverage validation

- [x] 7.1 **Acceptance criterion** — with the §6.1 `thresholds` added, `npm run test:coverage` passes the `perFile` gate with zero "does not meet" errors: every covered file at `lines ≥ 98 / statements ≥ 98 / branches ≥ 95 / functions = 100`, with zero `/* v8 ignore */` added (or each one carrying a named rationale per TESTING.md if genuinely unreachable). Record the final file/test counts.
  - **Result:** `npm run test:coverage` green — 167 test files / 1999 tests pass, **zero** "does not meet" errors. 14 source files floored by 14 new colocated `*.test.tsx` files; **zero** `/* v8 ignore */` annotations added.

## 8. Specs + governance bookkeeping

- [x] 8.1 `list-metadata` delta (`specs/list-metadata/spec.md`) — the ADDED form dispatch+navigation requirement; reaches canonical via archive-time rollup (no apply-time write to the active spec, per §7.11).
- [x] 8.2 `list-visibility` delta (`specs/list-visibility/spec.md`) — the ADDED `VisibilityPicker` optimistic-apply/rollback requirement; archive-time rollup.
- [x] 8.3 `testing-foundation` delta (`specs/testing-foundation/spec.md`) — Tier-2 carve-out bookkeeping (whole-file coverage + enumeration of the form-UI files; the `ListHeroSection` real-vs-mirror closure); archive-only per design D13. `openspec validate test-list-create-edit-form --strict` passes.
- [ ] 8.4 **On archive** (at `/opsx:archive`): flip `test-coverage/tasks.md` §9.2 to `[x]`. §7.2 / §7.3 (global complexity promotion + final coverage baseline) remain blocked on §9.3–§9.5 still being open — this sub-proposal alone does not clear them.

## 9. Pre-merge (four-gate)

- [x] 9.1 `npm run lint` passes — 0 errors, no new warnings (the 2 pre-existing warnings in `Avatar.tsx` / `seed-dev-users.ts` are untouched).
- [x] 9.2 `npx tsc --noEmit` passes with zero errors (including after the `ListHeader.tsx` deletion).
- [x] 9.3 `npm run test` (full suite) passes.
- [x] 9.4 `npm run build` completes successfully.
