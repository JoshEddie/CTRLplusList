## 1. Test scaffolding (jsdom render + RSC harness)

- [x] 1.1 Reuse the existing jsdom setup (`test/helpers/setup.ts`). Establish the per-surface mock kits, all reusing established in-directory/sibling precedents: an async-RSC kit (`vi.mock('@/lib/auth')`, `vi.mock('@/lib/dal')` stubbing the read(s) each shell calls, `vi.mock('next/navigation')` with `redirect()` throwing the `REDIRECT:${url}` sentinel via `vi.hoisted` — the exact `items/__tests__/page.test.tsx` idiom); a layout-shell kit (mock the async child page, render the shell, assert the `<main>` wrapper + composition + `metadata`); and a pure-render kit (`vi.mock('@/app/ui/components/ListCard')` / `vi.mock('../HistoryCard')` → markers, the in-directory `HistoryCard.test.tsx` precedent).
  - **Done:** each kit realized inline in its file (no shared harness module needed — see §5.3). The async-RSC kit mirrors `items/__tests__/page.test.tsx` (`vi.hoisted` `REDIRECT:${url}` sentinel); the pure-render kit mirrors `HistoryCard.test.tsx` (`@/app/ui/components/ListCard` / `../HistoryCard` markers); the layout-shell kit mirrors `settings/connections/__tests__/page.test.tsx` (`Page, { metadata }` import + child mock).
- [x] 1.2 Provide the shared Arrange factories: `makeSession({ email, name })` (the `next-auth` `Session` shape the shells `await auth()` into) and `makeViewer({ id })` (the `getUserIdByEmail` row). Hoist shared Arrange (mock defaults, `vi.clearAllMocks()`) into each file's `beforeEach`; per the duplication audit (§5.3), the typed `makeRow` row-fixture is extracted to a colocated `__tests__/test-helpers.tsx` per directory under CLAUDE.md's weight·drift·count judgment, while the session/viewer/sentinel Arrange stays inline (at most one async shell per directory).
  - **Done:** the session/viewer literals + `vi.clearAllMocks()` Arrange is inline in each file's `beforeEach`; the `makeRow` row-fixture is extracted to a per-directory `__tests__/test-helpers.tsx` (history + bookmarks) per the §5.3 disposition.

## 2. Async RSC shell tests

- [x] 2.1 `lists/bookmarks/__tests__/BookmarksPage.test.tsx` — `auth()` → `null` (and a session with no `user.email`) throws the `REDIRECT:/` sentinel and `getBookmarkedListsByUser` is not called; a session whose `getUserIdByEmail` → `null` throws the `REDIRECT:/` sentinel; a resolved viewer + a stubbed `getBookmarkedListsByUser` asserts the call args (`viewer.id`) and renders the real `BookmarksList` with one card per row (mocked `ListCollectionsNav` marker present). Covers the empty pass-through (`BookmarksList` receives `[]`). (LOCKS the `visit-history` "Bookmarking … survives in history" page render + `getBookmarkedListsByUser` wiring.)
- [x] 2.2 `lists/history/__tests__/HistoryPage.test.tsx` — the substance core (Decision 4): the same two redirect legs; a resolved viewer + a stubbed `getVisitHistoryByUser` asserts the call args (`viewer.id, { limit: 100 }`) and renders the real `HistoryList`; the **conditional `ClearHistoryButton`** (mocked marker, floored by 4.14) is **present** when `rows.length > 0` and **absent** when `rows.length === 0`. (LOCKS the `visit-history` "per-row remove and bulk clear" page-level affordance.)
- [x] 2.3 `purchased/__tests__/page.test.tsx` — the same two redirect legs (`getItemsByPurchased` not called on the first leg); a resolved viewer + a stubbed `getItemsByPurchased` asserts the call args (`user.id`), renders the real `Header` ("Purchased") + the mocked `Items` (asserting the forwarded `items`) inside `<main className="container container--items-library">`.

## 3. Pure layout / trivial shell tests

- [x] 3.1 `lists/bookmarks/__tests__/page.test.tsx` — the default `Page` export renders `<BookmarksPage />` (mock `../BookmarksPage`) inside `<main className="container container--list-collections">`; assert `metadata.title === 'Bookmarks'`.
- [x] 3.2 `lists/history/__tests__/page.test.tsx` — the default `Page` export renders `<HistoryPage />` (mock `../HistoryPage`) inside the same `<main>`; assert `metadata.title === 'Visit history'`.
- [x] 3.3 `purchased/__tests__/loading.test.tsx` — call `PurchasedLoading()` and assert `tree.type === LoadingIndicator` and `tree.props.size === 'page'` (the `items/__tests__/loading.test.tsx` element-shape precedent — no `render` of the real indicator for a one-line shell).

## 4. Pure render component tests

- [x] 4.1 `lists/bookmarks/__tests__/BookmarksList.test.tsx` — `rows.length === 0` renders the `<p className="bookmarks-empty">` empty message and no `role="list"` grid; `rows.length > 0` renders the `<ul className="list-card-grid" role="list">` with one `<li>` per row, each wrapping the mocked `ListCard` with `showOwner` (assert the forwarded `data-show-owner` + one card per row, keyed `${user_id}-${list_id}`). (Both branches of the length guard.)
- [x] 4.2 `lists/history/__tests__/HistoryList.test.tsx` — `rows.length === 0` renders the `<p className="history-empty">No visits yet.</p>` branch and no grid; `rows.length > 0` renders the `<ul className="list-card-grid" role="list">` with one mocked `HistoryCard` per row (the in-directory `HistoryCard.test.tsx` mock precedent). (Both branches of the length guard.)

## 5. Audit findings (recorded BEFORE coverage validation)

- [x] 5.1 **Dead-code audit** (Decision 2) — confirm all 8 files are live and record the importer chain: `bookmarks/page.tsx`, `history/page.tsx`, `purchased/page.tsx`, `purchased/loading.tsx` are Next App-Router entry points; `BookmarksPage`/`HistoryPage` ← their `page.tsx`; `BookmarksList`/`HistoryList` ← those async pages. **No deletion** (contrast 9.3). Confirm `tsc --noEmit` + `npm run build` stay green.
  - **Finding:** all 8 files confirmed live, importer chain as stated (verified by reading the source imports). `bookmarks/page.tsx` → `BookmarksPage` → `BookmarksList` → `ListCard`; `history/page.tsx` → `HistoryPage` → (`ListCollectionsNav` + `ClearHistoryButton`) + `HistoryList` → `HistoryCard`; `purchased/page.tsx` → `Header` + `Items`; `purchased/loading.tsx` is the route's Next `loading` boundary. No deletions. `tsc --noEmit` clean and `npm run build` exits 0 (the `/lists/bookmarks`, `/lists/history`, `/purchased` routes all compiled — only the pre-existing workspace-root lockfile warning remains).
- [x] 5.2 **Route-string audit** (Decision 7) — record that a grep of every active `spec.md` for `/lists/bookmarks`, `/lists/history`, and `/purchased` found every mention consistent with the live routes (`list-collections` sub-nav scenarios, `home-digest` "See all" targets, `app-frame` / `loading-indicator-system` route matching). **No drift finding** (contrast 9.4's stale `/u/[id]`); the absence-of-drift is recorded so a reviewer sees the audit was run.
  - **Finding:** confirmed — every active-spec mention of `/lists/bookmarks`, `/lists/history`, `/purchased` matches the live routes (`list-collections` sub-nav active-tab scenarios, `home-digest` Bookmarks/Recently-visited "See all" targets, `app-frame` nav-pill route-prefix matching, `loading-indicator-system` `container--items-library` / route list). No spec prose contradicts the source under test. **No drift to flag.**
- [x] 5.3 **Duplication audit** — hoist shared Arrange into each file's `beforeEach`; extract a colocated `__tests__/test-helpers.tsx` per directory per CLAUDE.md's duplication judgment (weight · drift · count, not a raw copy count). Record the disposition.
  - **Finding:** the shared Arrange that matters is the `makeRow` row-fixture — a typed, multi-field factory that drifts **silently** (a changed default still compiles and still passes), so under the weight·drift·count judgment it is extracted to a colocated `__tests__/test-helpers.tsx` per directory: `lists/history/__tests__/test-helpers.tsx` (shared by `HistoryCard` [4.14], `HistoryList`, `HistoryPage` — three sharers) and `lists/bookmarks/__tests__/test-helpers.tsx` (`BookmarksList`, `BookmarksPage` — two sharers, but non-trivial and drift-prone, so extracted even at two). `purchased/__tests__/` has no row factory (its shell mocks `Items`), so no helper there. The `Bookmark` and `History` factories stay **separate** — different `*RowData` concepts, not merged into one helper (the original audit conflated "different types → don't merge" with "don't extract", and missed the same-type within-directory reuse). The session/viewer/`redirect`-sentinel Arrange stays inline in each file's `beforeEach` (at most one async shell per directory). Helpers live under `__tests__/`, so the existing `**/__tests__/**` coverage-exclude glob keeps them out of the report with no per-file config entry.
- [x] 5.4 **Complexity audit** — measure `sonarjs/cognitive-complexity` on the 8 covered files at HEAD; any function ≥ 15 is disposed by a single-file, behavior-preserving extraction covered by the new tests — never by raising the ceiling. Record each disposition (expect: no extraction needed — all 8 are small render/guard shells).
  - **Finding:** no extraction needed. With `sonarjs/cognitive-complexity` promoted to `error` (15) for all 8 files, `eslint` over the three carve-out directories reports **0 issues**. All 8 are small render/guard shells (the most-branching, `HistoryPage`, is well under the ceiling). No source modified.
- [x] 5.5 **Assertion audit** — every new test asserts an observable (exact rendered text/structure, the `redirect` sentinel, a forwarded prop, a read's call args, a count-branch class, the conditional-affordance presence/absence); no tautologies, execute-for-coverage, or snapshot-only tests. Confirm `vitest/expect-expect` and `vitest/valid-title` pass at `error`.
  - **Finding:** all 24 new tests assert observables — thrown `REDIRECT:/` sentinels (+ the read **not** called on the guard legs), the `getBookmarkedListsByUser('viewer')` / `getVisitHistoryByUser('viewer', { limit: 100 })` / `getItemsByPurchased('viewer')` call args, exact empty-message text (`No bookmarks yet…` / `No visits yet.`), `role="list"` grid presence/absence, per-row `listitem` / marker counts, the forwarded `data-show-owner` / `data-count`, the conditional `clear-history-button` marker presence-vs-absence, the `<main>` container classes, and `metadata.title`. No tautologies, execute-for-coverage, or snapshot-only tests; `eslint` (incl. `vitest/expect-expect` + `vitest/valid-title` at `error`) passes.
- [x] 5.6 **Invariant elevation** (Decision 6) — record the **no-elevation** decision with rationale: the Bookmarks page render + `favorited_at DESC` ordering is already the `visit-history` "Bookmarking … survives in history" SHALL (LOCKED); the History page render + bulk-clear is already the `visit-history` "per-row remove and bulk clear" SHALL (LOCKED); the section auth-guards, empty-state messages, and the Purchased shell's read-wiring/composition are small guards/shells with no owning capability — unit-covered, not elevated. No new requirement authored.
  - **Finding:** **no-elevation** confirmed. The Bookmarks page render + `getBookmarkedListsByUser` wiring is LOCKED by §2.1/§4.1 under the `visit-history` "Bookmarking … survives in history" SHALL; the History page render + conditional bulk-clear is LOCKED by §2.2/§4.2 under the `visit-history` "per-row remove and bulk clear" SHALL. The two-leg auth-guards, the empty-state messages, and the Purchased read-wiring/composition are small guards/shells with no clean owning capability (`server-endpoint-authorization` owns actions/routes, not these render guards; `getItemsByPurchased`'s spoiler/attribution invariant is a `list-item-management` SHALL owned by 4.9) — unit-covered, not elevated. No new requirement authored.

## 6. Enumeration + complexity promotion

- [x] 6.1 `vitest.config.ts`: ADD the 8 covered files to per-file `thresholds` at `COVERAGE_FLOOR` under a `// test-list-history-bookmark-pages (sub-proposal 9.5) — locked at universal COVERAGE_FLOOR.` comment.
- [x] 6.2 `eslint.config.mjs`: ADD the same 8 paths to the per-file `sonarjs/cognitive-complexity = error` array under the matching comment. Confirm `eslint .` reports no new issues (only the pre-existing `Avatar.tsx` / `seed-dev-users.ts` warnings remain).
  - **Result:** the 8 paths added under the matching comment. `eslint` over the carve-out directories → 0 issues; the full-project pre-existing `Avatar.tsx` (no-img-element) + `seed-dev-users.ts` (cognitive-complexity) warnings are untouched.

## 7. Coverage validation

- [x] 7.1 **Acceptance criterion** — with the §6.1 `thresholds` added, `npm run test:coverage` passes the `perFile` gate with zero "does not meet" errors: every covered file at `lines ≥ 98 / statements ≥ 98 / branches ≥ 95 / functions = 100`, with zero `/* v8 ignore */` added. Record the test-file / test counts (expect net **+8 jsdom test files**).
  - **Result:** `npm run test:coverage` exits 0 with zero "does not meet" errors. All 8 covered files measure **100/100/100/100** (statements/branches/functions/lines, per `coverage-summary.json`) — above the floor; **zero `/* v8 ignore */` added**. Counts: **2072 tests passing** (net **+24 tests** across **+8 jsdom test files**, up from the §9.4 baseline of 2048).

## 8. Specs + governance bookkeeping

- [x] 8.1 `testing-foundation` delta (`specs/testing-foundation/spec.md`) — Tier-2 carve-out bookkeeping (the 8 page-shell files whole-covered + enumerated); archive-only per design D13. No apply-time write to the active `testing-foundation` spec or the parent `test-coverage` accumulator. Confirm `openspec validate test-list-history-bookmark-pages --strict` passes.
  - **Result:** `openspec validate test-list-history-bookmark-pages --strict` → valid. No apply-time write to the active `testing-foundation` spec or the `test-coverage` accumulator (archive-only per D13).
- [x] 8.2 Confirm NO active capability spec changes at apply-time: the `visit-history` SHALLs are LOCKED as-is (asserted by the new tests, not modified); no spec-drift finding to flag (Decision 7).
  - **Result:** confirmed — no `visit-history` (or other active capability) spec edited; the only repo changes are the 8 `.test.tsx` files, the `vitest.config.ts` thresholds, and the `eslint.config.mjs` complexity-error entries. No source files modified.
- [x] 8.3 **On archive** (at `/opsx:archive`): flip `test-coverage/tasks.md` §9.5 to `[x]` with a landed/archived note. This is the **last** open §9 follow-up — on its archive, the §9 dependency on §7.1 clears, **unblocking** §7.2 / §7.3 / §7.5 / §8 of the governing change.
  - **Done (2026-06-09):** `test-coverage/tasks.md` §9.5 flipped to `[x]` with the landed/archived note (incl. the spec-review fixes — per-directory `makeRow` `test-helpers.tsx` extraction + the tightened CLAUDE.md/TESTING.md DRY rule); §7.2 / §7.3 / §7.5 / §8 of the governing change unblocked.

## 9. Pre-merge (five-gate)

- [x] 9.1 `npm run lint` passes — 0 errors, no new warnings (the pre-existing `Avatar.tsx` img-element + `seed-dev-users.ts` complexity warnings are untouched). → `eslint` over the carve-out dirs: 0 issues.
- [x] 9.2 `npx tsc --noEmit` passes with zero errors. → "No errors found".
- [x] 9.3 `npm run build` completes successfully (with DB env present). → exit 0; `/lists/bookmarks`, `/lists/history`, `/purchased` routes compiled (only the pre-existing lockfile workspace-root warning).
- [x] 9.4 `npm run test:coverage` (full suite) passes — all green including the +8 new files, every covered file at or above `COVERAGE_FLOOR`. → 2072 tests pass; the 8 files at 100/100/100/100.
- [x] 9.5 `npm run test:e2e` (Playwright) passes — zero failing tests (no e2e change here; re-run for defense in depth, also run per-PR by CI). → **18 passed (15.5s)**, exit 0.
