## 1. Test scaffolding (jsdom render + RSC harness)

- [x] 1.1 Reuse the existing jsdom setup (`test/helpers/setup.ts`) and `@testing-library/user-event`. Establish the per-surface mock kits: a client kit (`vi.mock('@/app/actions/user')` → `signInUser`/`signOutUser` spies, `vi.mock('next/image')` → plain `<img>`, `vi.mock('next/navigation')` where a component reads `useRouter`/`redirect`) and an async-RSC kit (`vi.mock('@/lib/auth')`, `vi.mock('next/server')` stubbing `connection()` to a resolved no-op for `SignInPage`, `vi.mock('next/navigation')` with `redirect()` throwing a sentinel — mirror `FollowingPage.test.tsx` / `ChooseItemsBody.test.tsx` / `AppFrame.test.tsx`).
  - **Disposition:** mock kits are inline per file (vi.mock hoists per module). The `redirect()` sentinel uses the `vi.hoisted(() => vi.fn(url => { throw new Error(\`REDIRECT:${url}\`) }))` pattern from `lists/new/__tests__/page.test.tsx`. `next/server`'s `connection()` is mocked to `vi.fn().mockResolvedValue(undefined)` for `SignInPage`.
- [x] 1.2 Provide a `Session` fixture factory (a `makeSession({ name, email, image })` returning the `next-auth` `Session` shape `User`/`UserMenu`/`UserAvatarPopover` read). Hoist shared Arrange (mock defaults, `vi.clearAllMocks()`) into each file's `beforeEach`; extract a colocated `app/(auth)/ui/components/__tests__/test-helpers.ts` only if the duplication audit finds 3+ reuse (per TESTING.md).
  - **Disposition:** colocated `app/(auth)/ui/components/__tests__/test-helpers.tsx` extracted (it carries `makeSession`, `MockNextImage`, `MockNextLink`). The `next/image` mock is reused by `UserImage` / `UserMenu` / `UserAvatarPopover` / `SignInPage` (4 files) and `MockNextLink` by `UserAvatarPopover` — past the 3+ threshold. `__tests__/**` is already in `coverage.exclude`, so the helper does not pollute coverage.

## 2. Async RSC shell tests (`app/(auth)/ui/components/__tests__/`, `app/(auth)/sign-in/__tests__/`)

- [x] 2.1 `User.test.tsx` — `auth()` resolving a session forwards it as the `session` prop to `UserMenu` (mock `./UserMenu`, assert the forwarded prop); `auth()` resolving `null` forwards `session={null}`. Await `User()` per the async-RSC pattern. (Lock the actual behavior; the `'use server'` directive misuse is recorded in §5, not changed here — Decision 7.)
  - **Note:** the `'use server'` directive is a harmless top-level string under vitest (`@vitejs/plugin-react` does not transform it — it is only meaningful to the RSC bundler), so `await User()` resolves and renders normally. The forwarded session is asserted by the `UserMenu` stub rendering `session.user.email` ("ada@example.com") vs "no-session".
- [x] 2.2 `SignInPage.test.tsx` — already-authenticated (`auth()` → session with user) throws the `redirect('/')` sentinel and renders no sign-in UI; session-less (`auth()` → `null`) renders the real `AuthContainer` + the logo `Image` (`alt="Ctrl+List"`) + the `Suspense`-wrapped `SignInButton` ("Sign in with Google"). `connection()` is mocked to a resolved no-op. This is the unit complement to the e2e (Decision 5) — owns the redirect branch the guest e2e does not exercise; LOCKS the `e2e-critical-flows` sign-in-surface render at the component level.
- [x] 2.3 `app/(auth)/sign-in/__tests__/page.test.tsx` — the `IndexPage` default export renders `SignInPage` inside `<Suspense>` with the `'loading sign in...'` fallback (mock `SignInPage`; await the async component).

## 3. Client component tests (`app/(auth)/ui/components/__tests__/`)

- [x] 3.1 `UserMenu.test.tsx` — signed-in (`session.user` present) renders `UserAvatarPopover` and NOT the `SignedOutMenu` placeholder (mock `./UserAvatarPopover`, assert it receives the `user`); signed-out (`session` null / no user) renders the `SignedOutMenu` "Sign In" placeholder. LOCKS the session→surface branch (non-elevated per Decision 6 — covered here, not specced).
- [x] 3.2 `UserMenu.test.tsx` — `SignedOutMenu` interaction: clicking the placeholder toggles the `show`/`hide` class state on the avatar container and the `AuthContainer` (`user-menu show`); clicking the close button (`LuX`) hides it again. Renders through the real `buttonClasses` (`variant: 'on-dark'`) and `AuthContainer`; the nested `SignInButton` is mocked at the boundary.
- [x] 3.3 `UserAvatarPopover.test.tsx` — closed by default (`aria-expanded="false"`); clicking the avatar trigger (`aria-haspopup="menu"`, `aria-label="User menu"`) opens the real `<Menu>`; the popover header renders `user.name` and `user.email`, with the `?? 'Signed in'` name-absent branch and the email-absent branch both covered. The Connections `MenuLinkItem` points at `/settings/connections`; the Sign-out submit `MenuItem` ("Sign out") sits inside a `signOutUser` form. **LOCKS** the existing `menu-system` "UserAvatarPopover uses Menu primitive" requirement.
- [x] 3.4 `UserAvatarPopover.test.tsx` — dismissal: Escape closes the menu (`aria-expanded` flips back) via the real `Menu onClose`; re-clicking the trigger toggles it closed. (Renders through the real `Menu`/`MenuLinkItem`/`MenuItem` — none re-owned. The `close` callback is exercised by Escape; the toggle updater by open + re-click.)
- [x] 3.5 `SignInButton.test.tsx` — renders a `<form>` wrapping the `gsi-material-button` button, the Google `<svg>`, and the exact "Sign in with Google" label (the visible `gsi-material-button-contents` span). Substance = exact rendered structure/text (Decision 4).
  - **Note:** the `user.click` → `expect(signInUser).toHaveBeenCalledTimes(1)` assertion (the bonus behavioral check) **fires reliably under React 19 + jsdom** — verified passing — so it is kept, not dropped.

## 4. Pure render component tests (`app/(auth)/ui/components/__tests__/`)

- [x] 4.1 `UserImage.test.tsx` — renders the `next/image` avatar with `alt={name}` and `src={image}`; covers the empty-string fallbacks (`image=''`, `name=''`) `UserAvatarPopover` passes via `user.image || ''` / `user.name || ''`. (jsdom normalizes an empty `src` to null, so the empty case asserts the `.avatar` class + empty `alt` only.)
- [x] 4.2 `AuthContainer.test.tsx` — renders the `sign-in-page` / `auth-container` wrapper around its children; covers both the `className`-provided branch (`sign-in-page user-menu show`) and the omitted branch (the `${className}` → `undefined` interpolation in the class list).

## 5. Dead-code disposition + audit findings (recorded BEFORE coverage validation)

- [x] 5.1 **Dead-code audit** — DELETE the six-file old-chrome cluster (Decision 2), each verified importer-less by repo-wide grep: `app/ui/components/AuthPage.tsx`, `app/ui/components/AppMenu.tsx`, `app/ui/components/Logo.tsx`, `app/ui/components/Nav.tsx`, `app/(auth)/ui/components/AuthButtons.tsx`, `app/(auth)/ui/components/SignOutButton.tsx`. Delete the orphaned `app/ui/components/__tests__/{AppMenu,Logo,Nav}.test.tsx`. Confirm `tsc --noEmit` + `npm run build` stay green after removal. Record the cascade (each file dead once its sole importer is removed).
  - **Finding:** all six source files + three orphaned test files deleted. Cascade confirmed: `AuthPage` (0 importers) → `AppMenu` (only `AuthPage`) → `Logo`/`Nav` (only `AppMenu`); `AuthButtons`/`SignOutButton` independently importer-less. `npx tsc --noEmit` clean and `npm run build` compiles successfully post-removal (the build's later page-data step needs a `DATABASE_URL`, an environmental requirement unrelated to this change — verified green with a connection string set).
- [x] 5.2 **Path / naming resolution** — record that the issue's named components live under `app/(auth)/ui/components/` (not `app/(auth)/sign-in/`, which holds only `page.tsx`); that `AuthButtons` / `SignOutButton` are the dead duplicates removed in §5.1; and that `AuthContainer.tsx` (a live, issue-unnamed member of the set) is added to the carve-out so its directory is not left at 0%.
  - **Finding:** confirmed — recorded as stated; `AuthContainer.tsx` floored alongside the issue-named files.
- [x] 5.3 **`'use server'` directive finding** (Decision 7) — record that `User.tsx` carries a top-level `'use server'` directive but default-exports a JSX-returning component, not a server action. The tests lock the actual render behavior; recommended operator disposition: drop the `'use server'` line (`User` is a plain async RSC). Not changed here (a directive edit is outside this test-only carve-out).
  - **Operator-decision finding:** `app/(auth)/ui/components/User.tsx:1` declares `'use server'` yet exports an async React component returning `<UserMenu session>`. `'use server'` marks a module's exports as server actions, not server components; the component only works because it is rendered as an RSC child of `AppFrame`. Recommended disposition: remove the `'use server'` line. Locked as-shipped by the test; not changed here.
- [x] 5.4 **`e2e-critical-flows` stale-prose finding** (Decision 8) — record that the active `e2e-critical-flows` spec and `e2e/auth.guest.spec.ts` call the sign-in surface "the AuthPage sign-in UI"; after the `AuthPage.tsx` deletion that prose dangles (the real surface is `SignInPage` via `/sign-in`). The e2e test/flow are unaffected. Recommended disposition: refresh the prose at the §7 close-out. Not modified here (cross-tier scope).
  - **Finding:** the e2e guest spec navigates to `/sign-in` (→ `SignInPage`), never importing `AuthPage`, so the deletion leaves the flow green; only the descriptive name "AuthPage sign-in UI" in `e2e-critical-flows/spec.md` and the test comment dangles. Flagged for an operator prose refresh; not edited here.
- [x] 5.5 **Duplication audit** — hoist shared Arrange into each file's `beforeEach`; extract a colocated `__tests__/test-helpers.ts` (`makeSession`, the `next/image` mock) only on 3+ reuse (per TESTING.md). Record the disposition.
  - **Finding:** `makeSession` + `MockNextImage` + `MockNextLink` extracted to the colocated `test-helpers.tsx` (4+ consumers). Per-file mock defaults / `vi.clearAllMocks()` hoisted into each `beforeEach`.
- [x] 5.6 **Complexity audit** — measure `sonarjs/cognitive-complexity` on the 8 covered files at HEAD; any function ≥ 15 (only `UserMenu.tsx` / `UserAvatarPopover.tsx` branch) is disposed by a single-file, behavior-preserving extraction covered by the new tests — never by raising the ceiling. Record each disposition.
  - **Finding:** `eslint` with `sonarjs/cognitive-complexity` promoted to `error` (ceiling 15) reports **no issues** across all 8 covered files — every function measures under 15. No extraction needed; the ceiling was not raised.
  - **Redundant-guard simplification:** `UserMenu.tsx` had `const userSignedIn = !!session?.user; ... if (userSignedIn && user)` — a redundant guard (per CLAUDE.md), since `userSignedIn` true implies `user` truthy, leaving the `user`-falsy branch uncoverable. Simplified to `const user = session?.user; if (user)` — behavior-preserving (the `&& user` only existed for TS narrowing, which `if (user)` provides directly) and covered by the new tests. This removed the dead branch rather than ignoring it.
- [x] 5.7 **Assertion audit** — every new test asserts an observable (exact rendered text/structure, forwarded prop, redirect sentinel, `aria-expanded` flip, link `href`, or class branch); no tautologies, execute-for-coverage, or snapshot-only tests. Record the §5.6 result and that `SignInButton`'s action-fired assertion (Decision 4) is kept only if it fires deterministically.
  - **Finding:** every test constrains an observable — the forwarded `session` (via email), the `redirect('/')` sentinel, the popover `aria-expanded` toggle, the rendered name/email/Connections-`href`/Sign-out, the `show`/`hide` class transitions, and the `signInUser` dispatch on submit. No execute-for-coverage / tautology / snapshot-only tests. `SignInButton`'s action-fired assertion fires deterministically under React 19 + jsdom and is kept (§3.5).
- [x] 5.8 **Invariant elevation** (Decision 6) — record the **no-elevation** decision with rationale: `UserAvatarPopover` structure is already a `menu-system` SHALL (LOCKED, not duplicated); the sign-in-surface render is already an `e2e-critical-flows` SHALL (LOCKED at the unit level, coordinated non-redundantly); the `UserMenu` session-branch and `SignInPage` redirect guard are small UI guards with no clean owning capability — fully unit-covered, not elevated.
  - **Finding:** no new requirement authored. `UserAvatarPopover` (menu-system) and the sign-in-surface render (e2e-critical-flows) are LOCKED by the new tests; the `UserMenu` session-branch and `SignInPage` redirect are unit-covered guards with no owning capability — deliberately not elevated.

## 6. Enumeration + complexity promotion

- [x] 6.1 `vitest.config.ts`: ADD the 8 live files to per-file `thresholds` at `COVERAGE_FLOOR` under a `// test-account-auth-ui (sub-proposal 9.3) — locked at universal COVERAGE_FLOOR.` comment; REMOVE the `app/ui/components/AppMenu.tsx` / `Logo.tsx` / `Nav.tsx` lines from the 4.1 block (leave `AppFrame.tsx` / `AppNav.tsx` / `AppLogo.tsx` / `Header.tsx` — all live).
- [x] 6.2 `eslint.config.mjs`: ADD the same 8 paths to the per-file `sonarjs/cognitive-complexity = error` array under the matching comment; REMOVE the `AppMenu.tsx` / `Logo.tsx` / `Nav.tsx` lines from the 4.1 block. `eslint .` reports no new issues (0 errors; only the 2 pre-existing `Avatar.tsx` / `seed-dev-users.ts` warnings remain).

## 7. Coverage validation

- [x] 7.1 **Acceptance criterion** — with the §6.1 `thresholds` added (and the three cluster entries removed), `npm run test:coverage` passes the `perFile` gate with zero "does not meet" errors: every covered file at `lines ≥ 98 / statements ≥ 98 / branches ≥ 95 / functions = 100`, with zero `/* v8 ignore */` added.
  - **Result:** `vitest run --coverage` green (exit 0) — **172 test files / 2000 tests pass, zero "does not meet" errors**. 8 source files floored by 8 new colocated `*.test.tsx` files (+1 colocated `test-helpers.tsx`); net **+8 jsdom test files, −3** (deleted cluster tests). **Zero** `/* v8 ignore */` annotations added.

## 8. Specs + governance bookkeeping

- [x] 8.1 `testing-foundation` delta (`specs/testing-foundation/spec.md`) — Tier-2 carve-out bookkeeping (the 8 live files whole-covered + enumerated; the old-chrome cluster removal); archive-only per design D13. No apply-time write to the active `testing-foundation` spec or the parent `test-coverage` accumulator. `openspec validate test-account-auth-ui --strict` passes.
- [x] 8.2 Confirm NO active capability spec changes at apply-time: `menu-system` and `e2e-critical-flows` SHALLs are LOCKED as-is (asserted by the new tests, not modified); `app-frame` is untouched (never named the deleted primitives).
- [x] 8.3 **On archive** (at `/opsx:archive`): flip `test-coverage/tasks.md` §9.3 to `[x]` with a landed/archived note. §7.2 / §7.3 (global complexity promotion + final coverage baseline) remain blocked on §9.4 / §9.5 still being open — this sub-proposal alone does not clear them. *(Deferred to `/opsx:archive`; not part of apply.)*

## 9. Pre-merge (four-gate)

- [x] 9.1 `npm run lint` passes — 0 errors, no new warnings (the pre-existing `Avatar.tsx` img-element + `seed-dev-users.ts` complexity warnings are untouched).
- [x] 9.2 `npx tsc --noEmit` passes with zero errors (including after the six-file cluster deletion).
- [x] 9.3 `npm run test` (full suite) passes — 2000 tests green.
- [x] 9.4 `npm run build` completes successfully (with DB env present; the bare-shell failure is the environmental missing-`DATABASE_URL` at page-data collection, not a code defect — compilation + type validation pass either way).
