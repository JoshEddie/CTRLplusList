<!-- Empty by gate 1, not by absence of decisions. This change's two candidates —
     the fork's declaration position, and the CLAUDE.md-vs-ADR boundary — are
     each already stated WITH their reasoning at the point they apply: a YAML
     comment beside the declaration, and the `adr` artifact instruction that
     ships with generation. A channel carrying rule and context together leaves
     an entry nothing to add, so neither is entered. See design.md, "The library
     ships empty; no backfill". -->

## ADDED ADRs

## MODIFIED ADRs

## REMOVED ADRs
