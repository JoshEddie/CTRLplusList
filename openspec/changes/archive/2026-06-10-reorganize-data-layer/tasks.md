# Tasks — reorganize-data-layer

Pure move: every function body, signature, schema, auth check, cache tag, and `updateTag` call relocates verbatim. Sequencing follows design D7 (shim-first); every numbered group leaves the tree compiling and tests green. Decomposition follows the size bands in design D11 (>500 split / 300–500 easy wins / goal <300); no module may land over 500 lines.

## 1. Baseline audit

- [x] 1.1 Record the pre-move cache-tag inventory: per-tag counts of `cacheTag(...)` and `updateTag(...)` call sites (`lists`, `items`, `list_visits`, `user_follows`, `user_blocks`) across `lib/dal.ts` and `app/actions/*.ts`. Note the counts in this file under this task for the 6.1 comparison.
  - Baseline (2026-06-09): `cacheTag` in `lib/dal.ts` — `lists`×4, `items`×5, `list_visits`×1, `user_follows`×2, `user_blocks`×1 (13 total). `updateTag` — `items`×9 (7 in items.ts; 2 in lists.ts from setListItems/updatePriority), `lists`×5, `list_visits`×4, `user_follows`×4, `user_blocks`×2 (24 total). Zero `revalidateTag` calls.

## 2. Move reads into `lib/data/` (design D3, D5, D11)

- [x] 2.1 Create `lib/data/user.ts` (~265 lines): move `getUserById`, `getUserIdByEmail`, `getProfileForUser`, `getFollowingByUser`, `getFollowersOfUser`, `isFollowing`, `viewerHasAnyFollows`, `hasBlocked`, `getBlockedByUser`, `getFollowingFeedUsers` verbatim from `lib/dal.ts` (preserving `'use cache'` / `cacheTag` / `react` `cache()` wrappers exactly). The exported `authedUserId` helper (the byte-identical private copy lifted from `app/actions/lists.ts` / `follows.ts`) landed in the internal module `lib/data/user.session.ts` instead, per revised D5 — keeping the NextAuth module-scope side effect of `@/lib/auth` out of the read module.
- [x] 2.2 Create `lib/data/list.ts` (~160): move `getList`, `getLists`, `getListsByUser`, `getListsSharedByUser`, `getPublicListsByUser`; `withVisibility` moves here as an **export** (consumed by `visit.ts` too).
- [x] 2.3 Create `lib/data/visit.ts` (~110): move `getBookmarkedListsByUser`, `getBookmarkStatus`, `getVisitHistoryByUser` plus private `withNestedListVisibility` (importing `withVisibility` from `@/lib/data/list`).
- [x] 2.4 Create `lib/data/purchase.ts` (~85): move `getItemsByPurchased`; `sanitizePurchases` moves here as an **export** plus private `firstNameOf`.
- [x] 2.5 Create `lib/data/item.ts` (~160): move `getItemsByUser`, `getItemById`, `getItemsByListId` (importing `sanitizePurchases` from `@/lib/data/purchase`).
- [x] 2.6 Reduce `lib/dal.ts` to a plain re-export shim over the five read modules. Verify: `npx tsc --noEmit` clean and the full vitest suite green (existing dal tests pass through the shim).

## 3. Move actions into `lib/data/` (design D1, D4, D5, D11)

- [x] 3.1 Create `lib/data/user.actions.ts` (~210, module-level `'use server'`): `signInUser`, `signOutUser` from `app/actions/user.ts`; `followUser`, `unfollowUser`, `removeFollower`, `blockUser`, `unblockUser` and its `ActionResponse` type from `app/actions/follows.ts`; its private `authedUserId` copy is replaced by the shared import from `@/lib/data/user.session`.
- [x] 3.2 Create `lib/data/item.schema.ts` (~100, internal, no directive): `ItemSchema` + `ItemData` from `app/actions/items.ts` lines 41–126.
- [x] 3.3 Create `lib/data/item.associations.ts` (~195, internal, no directive): `updateItemStores`, `updateItemLists` (now **exported**) + private `emptyStore` from items.ts lines 470–657. These MUST NOT land in any `'use server'` module — exporting them there would mint new endpoints (design D3 note).
- [x] 3.4 Create `lib/data/purchase.actions.ts` (~280, module-level `'use server'`): `createPurchase`, `removePurchase`; private `resolveClaimIdentity`, `canRemovePurchase`, `RemovePurchaseInput`, and `PG_UNIQUE_VIOLATION` if purchase-only (items.ts lines 143–389). The inline capacity-race comment moves with `createPurchase`.
- [x] 3.5 Create `lib/data/item.actions.ts` (~325, module-level `'use server'`): `getItemEditData`, `createItem`, `updateItem`, `archiveItem`, `deleteItem`; its `ActionResponse`; imports from `item.schema.ts` and `item.associations.ts`.
- [x] 3.6 Create `lib/data/visit.actions.ts` (~170, module-level `'use server'`): `bookmarkList`, `unbookmarkList`, `clearVisitHistory`, `removeVisit` (lists.ts lines 364–513), importing `authedUserId`.
- [x] 3.7 Create `lib/data/listItems.actions.ts` (~320, module-level `'use server'`): `setListItems`, `updatePriority`; private `checkListBalance`, `rebalanceList`, `reorderPosition` (lists.ts lines 514–812).
- [x] 3.8 Create `lib/data/list.actions.ts` (~345, module-level `'use server'`): `createList`, `updateList`, `deleteList`, `setListVisibility`; `ListSchema`, `VisibilitySchema`, its `ActionResponse`.
- [x] 3.9 Reduce `app/actions/{user,follows,lists,items}.ts` to plain re-export shims (functions and types, no directive; `lists.ts` re-exports from `list.actions`, `listItems.actions`, and `visit.actions`; `items.ts` from `item.actions`, `purchase.actions`, and `item.schema`). Verify: `npx tsc --noEmit` clean, full vitest suite green, and no `lib/data/` module exceeds 500 lines (`wc` via editor or Read tool).
- [x] 3.10 Dev-server smoke test of client-imported actions through the shims (`npm run dev:local` + preview: create a list, reorder items, claim an item, follow a user). If the bundler rejects re-exported action references, fall back to design D7's per-domain one-shot caller rewrite (skip the action shims) and record the outcome here.
  - Outcome (2026-06-09): the bundler accepts re-exported action references through the non-directive shims — no D7 fallback needed. Verified end-to-end as `dev-test-viewer`: home rails render (reads through the `@/lib/dal` shim); `createList` ("Shim Smoke Test" → redirect to choose-items); `setListItems` (added 2 items, list page shows them); `createPurchase` + `removePurchase` (claim → "You claimed this" → undo restores "Claim this gift"); `unfollowUser` + `followUser` (Following → Follow → Following on Bob's list). Zero browser-console and zero server errors throughout.

## 4. Relocate tests and consolidate fixtures (design D8, D9)

- [x] 4.1 Create `lib/data/__tests__/test-helpers.ts` merging `test/helpers/seedItemGraph.ts` and `app/actions/__tests__/test-helpers.ts`. Defaults are explicit; where the merged default would change what a relocated test seeds (e.g. `quantity_limit` 1 vs unset), the call site passes the value explicitly. Zero assertion edits in consuming tests.
- [x] 4.2 Move `test/helpers/seedVisitGraph.ts` → `lib/data/__tests__/seedVisitGraph.ts` (its only consumers relocate into this directory; single-directory helper rule, testing-foundation).
- [x] 4.3 Create `lib/data/__tests__/user.test.ts` from `lib/__tests__/dal.user.test.ts` + `dal.following.test.ts`, importing `@/lib/data/user`.
- [x] 4.4 Create `lib/data/__tests__/user.actions.test.ts` from `app/actions/__tests__/follows.test.ts` + `user.test.ts`, importing `@/lib/data/user.actions`.
- [x] 4.5 Create `lib/data/__tests__/list.test.ts` from `lib/__tests__/dal.list.test.ts`, importing `@/lib/data/list`.
- [x] 4.6 Create `lib/data/__tests__/list.actions.test.ts` and `listItems.actions.test.ts` from `app/actions/__tests__/lists.test.ts`: the `setListItems`/`updatePriority` describe blocks go to `listItems.actions.test.ts`, the list-CRUD blocks to `list.actions.test.ts` (any bookmark/visit blocks belong to 4.7). Bodies verbatim.
- [x] 4.7 Create `lib/data/__tests__/visit.test.ts` from `lib/__tests__/dal.visit-history.test.ts` and `visit.actions.test.ts` from `app/actions/__tests__/visitHistory.actions.test.ts` (both 1:1 moves), importing `@/lib/data/visit` / `@/lib/data/visit.actions`. The four bookmark/visit describes from `lists.test.ts` also landed in `visit.actions.test.ts` (per 4.6), scoped under a wrapper describe with its own beforeEach since the two source harnesses seed and authenticate differently. Verified: the five relocated files run green — 130 tests (12+15+40+20+43); the `lists.test.ts` split sums to its source count of 79.
- [x] 4.8 Create `lib/data/__tests__/item.test.ts` and `purchase.test.ts` from `lib/__tests__/dal.item.test.ts`: the `getItemsByPurchased` describe blocks go to `purchase.test.ts`, the rest to `item.test.ts`. Bodies verbatim.
- [x] 4.9 Create `lib/data/__tests__/item.actions.test.ts` and `purchase.actions.test.ts` from `app/actions/__tests__/items.test.ts`: the `createPurchase`/`removePurchase` describe blocks go to `purchase.actions.test.ts`, the rest to `item.actions.test.ts` (this lane also covers `item.schema.ts` and `item.associations.ts` via `createItem`/`updateItem`). Bodies verbatim.
- [x] 4.10 Delete the ten superseded test files (`lib/__tests__/dal.*.test.ts`, `app/actions/__tests__/*.test.ts`), `app/actions/__tests__/test-helpers.ts`, and `test/helpers/seedItemGraph.ts`. Verify: `npm run test:coverage` green and every module under `lib/data/` clears the per-file floor (98/98/95/100) — including the internal modules `item.schema.ts` / `item.associations.ts`.
  - Verified (2026-06-09): 203 files / 2104 tests green (count-neutral vs the post-3.x baseline), per-file floor holds repo-wide; lowest `lib/data/` cell is `item.associations.ts` branches 96.87 ≥ 95. One importer outside the deleted set surfaced: `app/(main)/lists/[id]/__tests__/page.generateMetadata.test.ts` imported `seedList` from the deleted `app/actions/__tests__/test-helpers` — repointed to `@/lib/data/__tests__/test-helpers` (same cross-directory shape as before the move; the testing-foundation two-directory extraction rule says this fixture should eventually hoist to `test/helpers/` — flagged as follow-up, out of scope for the pure move).

## 5. Migrate callers and delete the old paths

- [x] 5.1 Rewrite the ~51 `@/lib/dal` importers to the per-domain read modules (`@/lib/data/{user,list,visit,item,purchase}`). Import-line edits only.
- [x] 5.2 Rewrite the ~42 `@/app/actions/*` importers to the per-domain action modules (`@/lib/data/*.actions`, plus `@/lib/data/item.schema` for `ItemData` importers; former `@/app/actions/lists` importers split across `list.actions` / `listItems.actions` / `visit.actions`, former `@/app/actions/items` importers across `item.actions` / `purchase.actions`; type imports such as `ActionResponse` follow their module). Import-line edits only.
  - 94 files rewritten (imports, `vi.mock` factories split per new module, the one `typeof import('@/lib/dal')` in `RecentlyVisitedRail.test.ts`, and `lib/listAccess.ts`'s relative `'./dal'` → `@/lib/data/user`); prettier run over every touched file. Full suite green after the rewrite: 2104/2104, 0 failed suites.
- [x] 5.3 Delete the shims: `lib/dal.ts` and the entire `app/actions/` directory. Verify: Grep finds zero `@/lib/dal` or `@/app/actions` specifiers anywhere; `npx tsc --noEmit` clean.
  - Verified (2026-06-09): grep across `app/ lib/ hooks/ test/ e2e/` returns zero `@/lib/dal` / `@/app/actions` / `'./dal'` hits; tsc clean; lint 0 errors with only the two recorded yellow size advisories.

## 6. Docs, spec hygiene, audits

- [x] 6.1 Cache-tag audit: per-tag `cacheTag` / `updateTag` call-site counts across `lib/data/` exactly match the 1.1 baseline.
  - Verified (2026-06-09): `cacheTag` — `lists`×4 (list.ts), `items`×5 (item.ts×3, purchase.ts×1, list.ts×1), `list_visits`×1 (visit.ts), `user_follows`×2 + `user_blocks`×1 (user.ts) = 13. `updateTag` — `items`×9 (item.actions×4, purchase.actions×3, listItems.actions×2), `lists`×5 (list.actions×4, listItems.actions×1), `list_visits`×4 (visit.actions), `user_follows`×4 + `user_blocks`×2 (user.actions) = 24. Zero `revalidateTag`. Exact match.
- [x] 6.2 Update `openspec/config.yaml` context: "Data reads in `lib/dal.ts`; mutations in `app/actions/`" → per-domain `lib/data/` module pairs; same for the data-freshness pointer that names `lib/dal.ts`.
- [x] 6.3 Editorial fix (design D12, flagged for reviewer veto): `openspec/specs/items-library-shell/spec.md` line 13's non-normative `app/actions/**` reference → `lib/data/*.actions.ts`.
- [x] 6.4 Grep `DATABASE.md`, `TESTING.md`, `CLAUDE.md`, `README*` for `lib/dal` / `app/actions` pointers; update any stale ones (e.g. list-item-management residual-race pointer text mirrors `DATABASE.md`).
  - Re-verified after 5.x (2026-06-09): one hit — CLAUDE.md's "Where extracted helpers live" example named `lib/dal.ts`; replaced with `lib/data/user.ts`. DATABASE.md / TESTING.md / README clean. Remaining `lib/dal` / `app/actions` mentions in `openspec/specs/` live specs are covered by this change's spec deltas and resolve at archive.
- [x] 6.5 Add the size-band lint rules to `eslint.config.mjs` (design D11): core `max-lines: ['error', { max: 500, skipBlankLines: true, skipComments: true }]` and `sonarjs/max-lines: ['warn', { maximum: 300 }]`, scoped to production source (`app/**`, `lib/**`, `hooks/**`, `db/**`) with exemptions for `**/*.test.*`, `**/__tests__/**`, and `app/changelog/releases.ts` (`test/**`, `e2e/**`, `scripts/**` fall outside the `files` globs). Both rules count lines of code — comments/blanks free — so the two thresholds measure the same thing. Verified: `sonarjs/max-lines` exists in sonarjs@4 and fires on a 358-raw-line file; the moderate `lib/data` modules fall under 300 code lines and stay green.
- [x] 6.6 Run `npm run lint` and confirm: zero `max-lines` errors anywhere; `sonarjs/max-lines` warnings recorded. Actual yellow list (code-line counting): `app/api/image-search/route.ts`, `app/(main)/items/ui/components/itemform/useItemForm.ts` — two files, not the six predicted from raw `wc -l` counts (comments/blanks are free). Re-confirmed at the 7.1 gate.
- [x] 6.7 Add a "File size" subsection to `CLAUDE.md` under "Writing code": red >500 = lint error (split by table-cohesion/domain), yellow 300–500 = warning (pull easy wins; cohesive files may stay), green <300 = goal. Keep it brief and point at `eslint.config.mjs` and the `testing-foundation` spec requirement as the canonical homes.

## 7. Pre-merge

- [x] 7.1 `npm run lint` — zero errors; the only warnings are the yellow-band size advisories recorded in 6.6 (per the testing-foundation gate-interaction clause). Confirmed 2026-06-09: 0 errors, 2 warnings (`app/api/image-search/route.ts`, `useItemForm.ts`) — exactly the 6.6 list.
- [x] 7.2 `npx tsc --noEmit` — zero errors. Confirmed 2026-06-09.
- [x] 7.3 `npm run build` — completes successfully (catches server/client boundary violations the move could introduce — this is the gate that proves the `'use server'` module split is sound). Confirmed 2026-06-09: webpack production build compiles, all 22 routes generate.
- [x] 7.4 `npm run test:coverage` — zero failing tests; per-file floor holds for every `lib/data/` module. Confirmed 2026-06-09: 203 files / 2104 tests green; `lib/data` block 100/99.72/100/100, lowest cell `item.associations.ts` branches 96.87 ≥ 95.
- [x] 7.5 `npm run test:e2e` — zero failing tests. Confirmed 2026-06-09: 18/18 passed (authenticated + guest projects).
