// Max image candidates stored per item — the single ceiling the extractor
// (product-fetch), the server schema, and the client editor all honor, so the
// three never disagree on the limit.
export const MAX_IMAGE_CANDIDATES = 10;
